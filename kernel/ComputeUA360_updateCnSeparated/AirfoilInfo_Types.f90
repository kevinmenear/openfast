!KGEN-generated Fortran source file 
  
!Generated at : 2026-04-25 14:35:22 
!KGEN version : 0.8.1 
  
!STARTOFREGISTRYGENERATEDFILE 'AirfoilInfo_Types.f90'
! WARNING This file is generated automatically by the FAST registry.
! Do not edit.  Your changes to this file will be lost.
! FAST Registry
!*********************************************************************************************************************************
! AirfoilInfo_Types
!.................................................................................................................................
! This file is part of AirfoilInfo.
! Copyright (C) 2012-2016 National Renewable Energy Laboratory
! Licensed under the Apache License, Version 2.0 (the "License");
! you may not use this file except in compliance with the License.
! You may obtain a copy of the License at
!     http://www.apache.org/licenses/LICENSE-2.0
! Unless required by applicable law or agreed to in writing, software
! distributed under the License is distributed on an "AS IS" BASIS,
! WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
! See the License for the specific language governing permissions and
! limitations under the License.
! W A R N I N G : This file was automatically generated from the FAST registry.  Changes made to this file may be lost.
!*********************************************************************************************************************************
!> This module contains the user-defined types needed in AirfoilInfo. It also contains copy, destroy, pack, and
!! unpack routines associated with each defined data type. This code is automatically generated by the FAST Registry.


!
!
!
!
!
!
!
!
!
MODULE AirfoilInfo_Types
!---------------------------------------------------------------------------------------------------------------------------------
    USE nwtc_library 
    USE kgen_utils_mod
    USE tprof_mod, ONLY: tstart, tstop, tnull, tprnt 
    USE kgen_utils_mod
    IMPLICIT NONE 
! =========  AFI_UA_BL_Type  =======
  TYPE, PUBLIC :: AFI_UA_BL_Type
    REAL(ReKi)  :: alpha0 = 0.0_ReKi      !< Angle of attack for zero lift (also used in HGM) [input in degrees; stored as radians]
    REAL(ReKi)  :: alpha1 = 0.0_ReKi      !< angle of attack at f = 0.7, approximately the stall angle; for alpha >= alpha0 [input in degrees; stored as radians]
    REAL(ReKi)  :: alpha2 = 0.0_ReKi      !< angle of attack at f = 0.7, approximately the stall angle; for alpha < alpha0 [input in degrees; stored as radians]
    REAL(ReKi)  :: eta_e = 0.0_ReKi      !< Recovery factor in the range [0.85 - 0.95] [-]
    REAL(ReKi)  :: C_nalpha = 0.0_ReKi      !< Cn slope for zero lift (used for Beddoes-Leishman unsteady aero) [1/rad]
    REAL(ReKi)  :: C_lalpha = 0.0_ReKi      !< Cl slope for zero lift (used for HGM unsteady aero only) -> calculated [1/rad]
    REAL(ReKi)  :: T_f0 = 0.0_ReKi      !< initial value of T_f, airfoil specific, used to compute D_f and fprimeprime (also used in HGM) [-]
    REAL(ReKi)  :: T_V0 = 0.0_ReKi      !< initial value of T_V, airfoil specific, time parameter associated with the vortex lift decay process, used in Cn_v [-]
    REAL(ReKi)  :: T_p = 0.0_ReKi      !< boundary-layer, leading edge pressure gradient time parameter; used in D_p; airfoil specific (also used in HGM) [-]
    REAL(ReKi)  :: T_VL = 0.0_ReKi      !< Initial value of the time constant associated with the vortex advection process; it represents the non-dimensional time in semi-chords, needed for a vortex to travel from LE to trailing edge (TE); it is used in the expression of Cvn. It depends on Re, M (weakly), and airfoil. [valid range = 6 - 13] [-]
    REAL(ReKi)  :: b1 = 0.0_ReKi      !< airfoil constant derived from experimental results (also used in HGM), usually 0.14 [-]
    REAL(ReKi)  :: b2 = 0.0_ReKi      !< airfoil constant derived from experimental results (also used in HGM), usually 0.53 [-]
    REAL(ReKi)  :: b5 = 0.0_ReKi      !< airfoil constant derived from experimental results, usually 5.0 [-]
    REAL(ReKi)  :: A1 = 0.0_ReKi      !< airfoil constant derived from experimental results (also used in HGM), usually 0.3 [-]
    REAL(ReKi)  :: A2 = 0.0_ReKi      !< airfoil constant derived from experimental results (also used in HGM), usually 0.7 [-]
    REAL(ReKi)  :: A5 = 0.0_ReKi      !< airfoil constant derived from experimental results, usually 1.0 [-]
    REAL(ReKi)  :: S1 = 0.0_ReKi      !< Constant in the f curve best-fit for alpha0<=AOA<=alpha1 [-]
    REAL(ReKi)  :: S2 = 0.0_ReKi      !< Constant in the f curve best-fit for         AOA> alpha1 [-]
    REAL(ReKi)  :: S3 = 0.0_ReKi      !< Constant in the f curve best-fit for alpha2<=AOA< alpha0 [-]
    REAL(ReKi)  :: S4 = 0.0_ReKi      !< Constant in the f curve best-fit for         AOA< alpha2 [-]
    REAL(ReKi)  :: Cn1 = 0.0_ReKi      !< Cn at stall value for positive angle of attack [or critical value of Cn_prime at LE separation for alpha >= alpha0] [-]
    REAL(ReKi)  :: Cn2 = 0.0_ReKi      !< Cn at stall value for negative angle of attack [or critical value of Cn_prime at LE separation for alpha < alpha0] [-]
    REAL(ReKi)  :: St_sh = 0.0_ReKi      !< Strouhal's shedding frequency constant. [-]
    REAL(ReKi)  :: Cd0 = 0.0_ReKi      !< Minimum Cd value [-]
    REAL(ReKi)  :: Cm0 = 0.0_ReKi      !< 2D pitching moment coefficient at zero lift, positive if nose is up [-]
    REAL(ReKi)  :: k0 = 0.0_ReKi      !< airfoil parameter in the x_cp_hat curve best-fit [ignored if UAMod<>1] [-]
    REAL(ReKi)  :: k1 = 0.0_ReKi      !< airfoil parameter in the x_cp_hat curve best-fit [ignored if UAMod<>1] [-]
    REAL(ReKi)  :: k2 = 0.0_ReKi      !< airfoil parameter in the x_cp_hat curve best-fit [ignored if UAMod<>1] [-]
    REAL(ReKi)  :: k3 = 0.0_ReKi      !< airfoil parameter in the x_cp_hat curve best-fit [ignored if UAMod<>1] [-]
    REAL(ReKi)  :: k1_hat = 0.0_ReKi      !< Constant in the expression of Cc due to leading edge vortex effects.  [ignored if UAMod<>1] [-]
    REAL(ReKi)  :: x_cp_bar = 0.0_ReKi      !< Constant in the expression of \hat(x)_cp^v [ignored if UAMod<>1, default = 0.2] [-]
    REAL(ReKi)  :: UACutout = 0.0_ReKi      !< Angle of attack above which unsteady aerodynamics are disabled [input in degrees; stored as radians]
    REAL(ReKi)  :: UACutout_delta = 0.0_ReKi      !< Number of angles-of-attack below UACutout where unsteady aerodynamics begin to be disabled [input in degrees; stored as radians]
    REAL(ReKi)  :: UACutout_blend = 0.0_ReKi      !< Angle of attack above which unsteady aerodynamics begins to be disabled [stored as radians]
    REAL(ReKi)  :: filtCutOff = 0.0_ReKi      !< Reduced frequency cutoff used to calculate the dynamic low pass filter cut-off frequency for the pitching rate and accelerations [default = 0.5] [-]
    REAL(ReKi)  :: alphaUpper = 0.0_ReKi      !< (input) upper angle of attack defining fully attached region [input in degrees; stored as radians]
    REAL(ReKi)  :: alphaLower = 0.0_ReKi      !< (input) lower angle of attack defining fully attached region [input in degrees; stored as radians]
    REAL(ReKi)  :: c_alphaLower = 0.0_ReKi      !< (calculated) value of cn or cl at alphaLower [-]
    REAL(ReKi)  :: c_alphaUpper = 0.0_ReKi      !< (calculated) value of cn or cl at alphaUpper [-]
    REAL(ReKi)  :: alpha0ReverseFlow = 0.0_ReKi      !< (calculated) Angle of attack for Cn=0 for reverse flow [rad]
    REAL(ReKi)  :: alphaBreakUpper = 0.0_ReKi      !< (calculated) Angle of attack where normal and reverse flow CnAttached intersect; between 0 and +pi; will be near +pi/2 deg in most cases [rad]
    REAL(ReKi)  :: CnBreakUpper = 0.0_ReKi      !< (calculated) CnAttached value at alphaBreakUpper where normal and reverse flow CnAttached intersect; will be positive [-]
    REAL(ReKi)  :: alphaBreakLower = 0.0_ReKi      !< (calculated) Angle of attack where normal and reverse flow CnAttached intersect; between -pi and 0; will be near -pi/2 deg in most cases [rad]
    REAL(ReKi)  :: CnBreakLower = 0.0_ReKi      !< (calculated) CnAttached value at alphaBreakLower where normal and reverse flow CnAttached intersect; will be negative [-]
  END TYPE AFI_UA_BL_Type
! =======================
! =========  AFI_UA_BL_Default_Type  =======

! =======================
! =========  AFI_Table_Type  =======
  TYPE, PUBLIC :: AFI_Table_Type
    REAL(ReKi) , DIMENSION(:), ALLOCATABLE  :: Alpha      !< Angle-of-attack vector that matches the Coefs matrix [rad]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Coefs      !< Airfoil coefficients for Cd, Cl,  and maybe Cm and/or Cpmin [-]
    REAL(ReKi) , DIMENSION(:,:,:), ALLOCATABLE  :: SplineCoefs      !< Spline coefficients for Cd, Cl,  and maybe Cm and/or Cpmin [-]
    REAL(ReKi)  :: UserProp = 0.0_ReKi      !< User Property for a table, for example a Control setting [-]
    REAL(ReKi)  :: Re = 0.0_ReKi      !< Reynolds number [-]
    INTEGER(IntKi)  :: NumAlf = 0_IntKi      !< Length of the Alpha and Coefs arrays [-]
    LOGICAL  :: ConstData = .false.      !< Flag that tells if aerodynamic coefficients are the same for all alphas [-]
    LOGICAL  :: InclUAdata = .false.      !< Flag that tells if UA data is included in the input file [-]
    TYPE(AFI_UA_BL_Type)  :: UA_BL      !< The tables of Leishman-Beddoes unsteady-aero data for given Re and control setting [-]
  END TYPE AFI_Table_Type
! =======================
! =========  AFI_InitInputType  =======

! =======================
! =========  AFI_InitOutputType  =======

! =======================
! =========  AFI_ParameterType  =======

! =======================
! =========  AFI_InputType  =======

! =======================
! =========  AFI_OutputType  =======

! =======================
  PUBLIC kr_airfoilinfo_types_afi_ua_bl_type 
  PUBLIC kr_airfoilinfo_types_afi_table_type 
  PUBLIC kv_airfoilinfo_types_afi_ua_bl_type 
  PUBLIC kv_airfoilinfo_types_afi_table_type 

    
  CONTAINS 
    


  !read state subroutine for kr_airfoilinfo_types_afi_ua_bl_type 
  RECURSIVE SUBROUTINE kr_airfoilinfo_types_afi_ua_bl_type(var, kgen_unit, printname, printvar) 
      TYPE(afi_ua_bl_type), INTENT(INOUT) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
        
      READ (UNIT = kgen_unit) var%alpha0 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%alpha0 = ", var%alpha0 
      END IF   
        
      READ (UNIT = kgen_unit) var%alpha1 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%alpha1 = ", var%alpha1 
      END IF   
        
      READ (UNIT = kgen_unit) var%alpha2 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%alpha2 = ", var%alpha2 
      END IF   
        
      READ (UNIT = kgen_unit) var%eta_e 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%eta_e = ", var%eta_e 
      END IF   
        
      READ (UNIT = kgen_unit) var%c_nalpha 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%c_nalpha = ", var%c_nalpha 
      END IF   
        
      READ (UNIT = kgen_unit) var%c_lalpha 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%c_lalpha = ", var%c_lalpha 
      END IF   
        
      READ (UNIT = kgen_unit) var%t_f0 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%t_f0 = ", var%t_f0 
      END IF   
        
      READ (UNIT = kgen_unit) var%t_v0 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%t_v0 = ", var%t_v0 
      END IF   
        
      READ (UNIT = kgen_unit) var%t_p 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%t_p = ", var%t_p 
      END IF   
        
      READ (UNIT = kgen_unit) var%t_vl 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%t_vl = ", var%t_vl 
      END IF   
        
      READ (UNIT = kgen_unit) var%b1 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%b1 = ", var%b1 
      END IF   
        
      READ (UNIT = kgen_unit) var%b2 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%b2 = ", var%b2 
      END IF   
        
      READ (UNIT = kgen_unit) var%b5 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%b5 = ", var%b5 
      END IF   
        
      READ (UNIT = kgen_unit) var%a1 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%a1 = ", var%a1 
      END IF   
        
      READ (UNIT = kgen_unit) var%a2 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%a2 = ", var%a2 
      END IF   
        
      READ (UNIT = kgen_unit) var%a5 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%a5 = ", var%a5 
      END IF   
        
      READ (UNIT = kgen_unit) var%s1 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%s1 = ", var%s1 
      END IF   
        
      READ (UNIT = kgen_unit) var%s2 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%s2 = ", var%s2 
      END IF   
        
      READ (UNIT = kgen_unit) var%s3 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%s3 = ", var%s3 
      END IF   
        
      READ (UNIT = kgen_unit) var%s4 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%s4 = ", var%s4 
      END IF   
        
      READ (UNIT = kgen_unit) var%cn1 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%cn1 = ", var%cn1 
      END IF   
        
      READ (UNIT = kgen_unit) var%cn2 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%cn2 = ", var%cn2 
      END IF   
        
      READ (UNIT = kgen_unit) var%st_sh 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%st_sh = ", var%st_sh 
      END IF   
        
      READ (UNIT = kgen_unit) var%cd0 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%cd0 = ", var%cd0 
      END IF   
        
      READ (UNIT = kgen_unit) var%cm0 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%cm0 = ", var%cm0 
      END IF   
        
      READ (UNIT = kgen_unit) var%k0 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%k0 = ", var%k0 
      END IF   
        
      READ (UNIT = kgen_unit) var%k1 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%k1 = ", var%k1 
      END IF   
        
      READ (UNIT = kgen_unit) var%k2 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%k2 = ", var%k2 
      END IF   
        
      READ (UNIT = kgen_unit) var%k3 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%k3 = ", var%k3 
      END IF   
        
      READ (UNIT = kgen_unit) var%k1_hat 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%k1_hat = ", var%k1_hat 
      END IF   
        
      READ (UNIT = kgen_unit) var%x_cp_bar 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%x_cp_bar = ", var%x_cp_bar 
      END IF   
        
      READ (UNIT = kgen_unit) var%uacutout 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%uacutout = ", var%uacutout 
      END IF   
        
      READ (UNIT = kgen_unit) var%uacutout_delta 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%uacutout_delta = ", var%uacutout_delta 
      END IF   
        
      READ (UNIT = kgen_unit) var%uacutout_blend 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%uacutout_blend = ", var%uacutout_blend 
      END IF   
        
      READ (UNIT = kgen_unit) var%filtcutoff 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%filtcutoff = ", var%filtcutoff 
      END IF   
        
      READ (UNIT = kgen_unit) var%alphaupper 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%alphaupper = ", var%alphaupper 
      END IF   
        
      READ (UNIT = kgen_unit) var%alphalower 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%alphalower = ", var%alphalower 
      END IF   
        
      READ (UNIT = kgen_unit) var%c_alphalower 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%c_alphalower = ", var%c_alphalower 
      END IF   
        
      READ (UNIT = kgen_unit) var%c_alphaupper 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%c_alphaupper = ", var%c_alphaupper 
      END IF   
        
      READ (UNIT = kgen_unit) var%alpha0reverseflow 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%alpha0reverseflow = ", var%alpha0reverseflow 
      END IF   
        
      READ (UNIT = kgen_unit) var%alphabreakupper 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%alphabreakupper = ", var%alphabreakupper 
      END IF   
        
      READ (UNIT = kgen_unit) var%cnbreakupper 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%cnbreakupper = ", var%cnbreakupper 
      END IF   
        
      READ (UNIT = kgen_unit) var%alphabreaklower 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%alphabreaklower = ", var%alphabreaklower 
      END IF   
        
      READ (UNIT = kgen_unit) var%cnbreaklower 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%cnbreaklower = ", var%cnbreaklower 
      END IF   
        
  END SUBROUTINE kr_airfoilinfo_types_afi_ua_bl_type 
    
  !read state subroutine for kr_airfoilinfo_types_afi_table_type 
  RECURSIVE SUBROUTINE kr_airfoilinfo_types_afi_table_type(var, kgen_unit, printname, printvar) 
      TYPE(afi_table_type), INTENT(INOUT) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_afi_table_type_real__reki_dim1(var%alpha, kgen_unit, printname // "%alpha", .TRUE.) 
      ELSE 
          CALL kr_afi_table_type_real__reki_dim1(var%alpha, kgen_unit, printname // "%alpha", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_afi_table_type_real__reki_dim2(var%coefs, kgen_unit, printname // "%coefs", .TRUE.) 
      ELSE 
          CALL kr_afi_table_type_real__reki_dim2(var%coefs, kgen_unit, printname // "%coefs", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_afi_table_type_real__reki_dim3(var%splinecoefs, kgen_unit, printname // "%splinecoefs", .TRUE.) 
      ELSE 
          CALL kr_afi_table_type_real__reki_dim3(var%splinecoefs, kgen_unit, printname // "%splinecoefs", .FALSE.) 
      END IF   
        
      READ (UNIT = kgen_unit) var%userprop 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%userprop = ", var%userprop 
      END IF   
        
      READ (UNIT = kgen_unit) var%re 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%re = ", var%re 
      END IF   
        
      READ (UNIT = kgen_unit) var%numalf 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%numalf = ", var%numalf 
      END IF   
        
      READ (UNIT = kgen_unit) var%constdata 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%constdata = ", var%constdata 
      END IF   
        
      READ (UNIT = kgen_unit) var%incluadata 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%incluadata = ", var%incluadata 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_airfoilinfo_types_afi_ua_bl_type(var%ua_bl, kgen_unit, printname // "%ua_bl", .TRUE.) 
      ELSE 
          CALL kr_airfoilinfo_types_afi_ua_bl_type(var%ua_bl, kgen_unit, printname // "%ua_bl", .FALSE.) 
      END IF   
        
  END SUBROUTINE kr_airfoilinfo_types_afi_table_type 
    
  !write state subroutine for kr_afi_table_type_real__reki_dim1 
  SUBROUTINE kr_afi_table_type_real__reki_dim1(var, kgen_unit, printname, printvar) 
      REAL(KIND=reki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1 
      INTEGER, DIMENSION(2,1) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_afi_table_type_real__reki_dim1 
    
  !write state subroutine for kr_afi_table_type_real__reki_dim2 
  SUBROUTINE kr_afi_table_type_real__reki_dim2(var, kgen_unit, printname, printvar) 
      REAL(KIND=reki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:,:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1, idx2 
      INTEGER, DIMENSION(2,2) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          READ (UNIT = kgen_unit) kgen_bound(1, 2) 
          READ (UNIT = kgen_unit) kgen_bound(2, 2) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1), kgen_bound(1,2):kgen_bound(2,2))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_afi_table_type_real__reki_dim2 
    
  !write state subroutine for kr_afi_table_type_real__reki_dim3 
  SUBROUTINE kr_afi_table_type_real__reki_dim3(var, kgen_unit, printname, printvar) 
      REAL(KIND=reki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:,:,:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1, idx2, idx3 
      INTEGER, DIMENSION(2,3) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          READ (UNIT = kgen_unit) kgen_bound(1, 2) 
          READ (UNIT = kgen_unit) kgen_bound(2, 2) 
          READ (UNIT = kgen_unit) kgen_bound(1, 3) 
          READ (UNIT = kgen_unit) kgen_bound(2, 3) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1), kgen_bound(1,2):kgen_bound(2,2), kgen_bound(1,3):kgen_bound(2,3))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_afi_table_type_real__reki_dim3 
    
  !verify state subroutine for kv_airfoilinfo_types_afi_ua_bl_type 
  RECURSIVE SUBROUTINE kv_airfoilinfo_types_afi_ua_bl_type(varname, check_status, var, kgenref_var) 
      CHARACTER(LEN=*), INTENT(IN) :: varname 
      TYPE(check_t), INTENT(INOUT) :: check_status 
      TYPE(afi_ua_bl_type), INTENT(IN) :: var, kgenref_var 
      TYPE(check_t) :: dtype_check_status, comp_check_status 
      INTEGER :: check_result 
      LOGICAL :: is_print = .FALSE. 
        
      real(KIND=reki) :: diff_alpha0 
      real(KIND=reki) :: diff_alpha1 
      real(KIND=reki) :: diff_alpha2 
      real(KIND=reki) :: diff_eta_e 
      real(KIND=reki) :: diff_c_nalpha 
      real(KIND=reki) :: diff_c_lalpha 
      real(KIND=reki) :: diff_t_f0 
      real(KIND=reki) :: diff_t_v0 
      real(KIND=reki) :: diff_t_p 
      real(KIND=reki) :: diff_t_vl 
      real(KIND=reki) :: diff_b1 
      real(KIND=reki) :: diff_b2 
      real(KIND=reki) :: diff_b5 
      real(KIND=reki) :: diff_a1 
      real(KIND=reki) :: diff_a2 
      real(KIND=reki) :: diff_a5 
      real(KIND=reki) :: diff_s1 
      real(KIND=reki) :: diff_s2 
      real(KIND=reki) :: diff_s3 
      real(KIND=reki) :: diff_s4 
      real(KIND=reki) :: diff_cn1 
      real(KIND=reki) :: diff_cn2 
      real(KIND=reki) :: diff_st_sh 
      real(KIND=reki) :: diff_cd0 
      real(KIND=reki) :: diff_cm0 
      real(KIND=reki) :: diff_k0 
      real(KIND=reki) :: diff_k1 
      real(KIND=reki) :: diff_k2 
      real(KIND=reki) :: diff_k3 
      real(KIND=reki) :: diff_k1_hat 
      real(KIND=reki) :: diff_x_cp_bar 
      real(KIND=reki) :: diff_uacutout 
      real(KIND=reki) :: diff_uacutout_delta 
      real(KIND=reki) :: diff_uacutout_blend 
      real(KIND=reki) :: diff_filtcutoff 
      real(KIND=reki) :: diff_alphaupper 
      real(KIND=reki) :: diff_alphalower 
      real(KIND=reki) :: diff_c_alphalower 
      real(KIND=reki) :: diff_c_alphaupper 
      real(KIND=reki) :: diff_alpha0reverseflow 
      real(KIND=reki) :: diff_alphabreakupper 
      real(KIND=reki) :: diff_cnbreakupper 
      real(KIND=reki) :: diff_alphabreaklower 
      real(KIND=reki) :: diff_cnbreaklower 
        
      check_status%numTotal = check_status%numTotal + 1 
        
      CALL kgen_init_check(dtype_check_status, rank=check_status%rank) 
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%alpha0 == kgenref_var%alpha0) .OR. ((var%alpha0 /= var%alpha0) .AND. (kgenref_var%alpha0 /= kgenref_var%alpha0))) THEN
        IF (var%alpha0 /= var%alpha0) WRITE(*, *) "localvar%alpha0 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%alpha0 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] alpha0 | IDENTICAL | ", var%alpha0, " | ", kgenref_var%alpha0
      ELSE 
          diff_alpha0 = ABS(var%alpha0 - kgenref_var%alpha0) 
          IF (diff_alpha0 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha0 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] alpha0 | IN_TOL | ", var%alpha0, " | ", kgenref_var%alpha0, " | ", diff_alpha0
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha0 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] alpha0 | OUT_TOL | ", var%alpha0, " | ", kgenref_var%alpha0, " | ", diff_alpha0
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alpha0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alpha0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%alpha1 == kgenref_var%alpha1) .OR. ((var%alpha1 /= var%alpha1) .AND. (kgenref_var%alpha1 /= kgenref_var%alpha1))) THEN
        IF (var%alpha1 /= var%alpha1) WRITE(*, *) "localvar%alpha1 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%alpha1 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] alpha1 | IDENTICAL | ", var%alpha1, " | ", kgenref_var%alpha1
      ELSE 
          diff_alpha1 = ABS(var%alpha1 - kgenref_var%alpha1) 
          IF (diff_alpha1 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha1 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] alpha1 | IN_TOL | ", var%alpha1, " | ", kgenref_var%alpha1, " | ", diff_alpha1
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha1 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] alpha1 | OUT_TOL | ", var%alpha1, " | ", kgenref_var%alpha1, " | ", diff_alpha1
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alpha1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alpha1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%alpha2 == kgenref_var%alpha2) .OR. ((var%alpha2 /= var%alpha2) .AND. (kgenref_var%alpha2 /= kgenref_var%alpha2))) THEN
        IF (var%alpha2 /= var%alpha2) WRITE(*, *) "localvar%alpha2 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%alpha2 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] alpha2 | IDENTICAL | ", var%alpha2, " | ", kgenref_var%alpha2
      ELSE 
          diff_alpha2 = ABS(var%alpha2 - kgenref_var%alpha2) 
          IF (diff_alpha2 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha2 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] alpha2 | IN_TOL | ", var%alpha2, " | ", kgenref_var%alpha2, " | ", diff_alpha2
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha2 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] alpha2 | OUT_TOL | ", var%alpha2, " | ", kgenref_var%alpha2, " | ", diff_alpha2
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alpha2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alpha2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%eta_e == kgenref_var%eta_e) .OR. ((var%eta_e /= var%eta_e) .AND. (kgenref_var%eta_e /= kgenref_var%eta_e))) THEN
        IF (var%eta_e /= var%eta_e) WRITE(*, *) "localvar%eta_e is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%eta_e is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] eta_e | IDENTICAL | ", var%eta_e, " | ", kgenref_var%eta_e
      ELSE 
          diff_eta_e = ABS(var%eta_e - kgenref_var%eta_e) 
          IF (diff_eta_e <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%eta_e is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] eta_e | IN_TOL | ", var%eta_e, " | ", kgenref_var%eta_e, " | ", diff_eta_e
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%eta_e is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] eta_e | OUT_TOL | ", var%eta_e, " | ", kgenref_var%eta_e, " | ", diff_eta_e
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_eta_e 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_eta_e 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%c_nalpha == kgenref_var%c_nalpha) .OR. ((var%c_nalpha /= var%c_nalpha) .AND. (kgenref_var%c_nalpha /= kgenref_var%c_nalpha))) THEN
        IF (var%c_nalpha /= var%c_nalpha) WRITE(*, *) "localvar%c_nalpha is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%c_nalpha is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] c_nalpha | IDENTICAL | ", var%c_nalpha, " | ", kgenref_var%c_nalpha
      ELSE 
          diff_c_nalpha = ABS(var%c_nalpha - kgenref_var%c_nalpha) 
          IF (diff_c_nalpha <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%c_nalpha is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] c_nalpha | IN_TOL | ", var%c_nalpha, " | ", kgenref_var%c_nalpha, " | ", diff_c_nalpha
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%c_nalpha is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] c_nalpha | OUT_TOL | ", var%c_nalpha, " | ", kgenref_var%c_nalpha, " | ", diff_c_nalpha
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_c_nalpha 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_c_nalpha 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%c_lalpha == kgenref_var%c_lalpha) .OR. ((var%c_lalpha /= var%c_lalpha) .AND. (kgenref_var%c_lalpha /= kgenref_var%c_lalpha))) THEN
        IF (var%c_lalpha /= var%c_lalpha) WRITE(*, *) "localvar%c_lalpha is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%c_lalpha is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] c_lalpha | IDENTICAL | ", var%c_lalpha, " | ", kgenref_var%c_lalpha
      ELSE 
          diff_c_lalpha = ABS(var%c_lalpha - kgenref_var%c_lalpha) 
          IF (diff_c_lalpha <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%c_lalpha is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] c_lalpha | IN_TOL | ", var%c_lalpha, " | ", kgenref_var%c_lalpha, " | ", diff_c_lalpha
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%c_lalpha is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] c_lalpha | OUT_TOL | ", var%c_lalpha, " | ", kgenref_var%c_lalpha, " | ", diff_c_lalpha
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_c_lalpha 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_c_lalpha 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%t_f0 == kgenref_var%t_f0) .OR. ((var%t_f0 /= var%t_f0) .AND. (kgenref_var%t_f0 /= kgenref_var%t_f0))) THEN
        IF (var%t_f0 /= var%t_f0) WRITE(*, *) "localvar%t_f0 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%t_f0 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] t_f0 | IDENTICAL | ", var%t_f0, " | ", kgenref_var%t_f0
      ELSE 
          diff_t_f0 = ABS(var%t_f0 - kgenref_var%t_f0) 
          IF (diff_t_f0 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%t_f0 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] t_f0 | IN_TOL | ", var%t_f0, " | ", kgenref_var%t_f0, " | ", diff_t_f0
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%t_f0 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] t_f0 | OUT_TOL | ", var%t_f0, " | ", kgenref_var%t_f0, " | ", diff_t_f0
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_t_f0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_t_f0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%t_v0 == kgenref_var%t_v0) .OR. ((var%t_v0 /= var%t_v0) .AND. (kgenref_var%t_v0 /= kgenref_var%t_v0))) THEN
        IF (var%t_v0 /= var%t_v0) WRITE(*, *) "localvar%t_v0 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%t_v0 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] t_v0 | IDENTICAL | ", var%t_v0, " | ", kgenref_var%t_v0
      ELSE 
          diff_t_v0 = ABS(var%t_v0 - kgenref_var%t_v0) 
          IF (diff_t_v0 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%t_v0 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] t_v0 | IN_TOL | ", var%t_v0, " | ", kgenref_var%t_v0, " | ", diff_t_v0
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%t_v0 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] t_v0 | OUT_TOL | ", var%t_v0, " | ", kgenref_var%t_v0, " | ", diff_t_v0
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_t_v0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_t_v0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%t_p == kgenref_var%t_p) .OR. ((var%t_p /= var%t_p) .AND. (kgenref_var%t_p /= kgenref_var%t_p))) THEN
        IF (var%t_p /= var%t_p) WRITE(*, *) "localvar%t_p is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%t_p is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] t_p | IDENTICAL | ", var%t_p, " | ", kgenref_var%t_p
      ELSE 
          diff_t_p = ABS(var%t_p - kgenref_var%t_p) 
          IF (diff_t_p <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%t_p is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] t_p | IN_TOL | ", var%t_p, " | ", kgenref_var%t_p, " | ", diff_t_p
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%t_p is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] t_p | OUT_TOL | ", var%t_p, " | ", kgenref_var%t_p, " | ", diff_t_p
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_t_p 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_t_p 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%t_vl == kgenref_var%t_vl) .OR. ((var%t_vl /= var%t_vl) .AND. (kgenref_var%t_vl /= kgenref_var%t_vl))) THEN
        IF (var%t_vl /= var%t_vl) WRITE(*, *) "localvar%t_vl is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%t_vl is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] t_vl | IDENTICAL | ", var%t_vl, " | ", kgenref_var%t_vl
      ELSE 
          diff_t_vl = ABS(var%t_vl - kgenref_var%t_vl) 
          IF (diff_t_vl <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%t_vl is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] t_vl | IN_TOL | ", var%t_vl, " | ", kgenref_var%t_vl, " | ", diff_t_vl
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%t_vl is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] t_vl | OUT_TOL | ", var%t_vl, " | ", kgenref_var%t_vl, " | ", diff_t_vl
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_t_vl 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_t_vl 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%b1 == kgenref_var%b1) .OR. ((var%b1 /= var%b1) .AND. (kgenref_var%b1 /= kgenref_var%b1))) THEN
        IF (var%b1 /= var%b1) WRITE(*, *) "localvar%b1 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%b1 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] b1 | IDENTICAL | ", var%b1, " | ", kgenref_var%b1
      ELSE 
          diff_b1 = ABS(var%b1 - kgenref_var%b1) 
          IF (diff_b1 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%b1 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] b1 | IN_TOL | ", var%b1, " | ", kgenref_var%b1, " | ", diff_b1
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%b1 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] b1 | OUT_TOL | ", var%b1, " | ", kgenref_var%b1, " | ", diff_b1
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_b1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_b1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%b2 == kgenref_var%b2) .OR. ((var%b2 /= var%b2) .AND. (kgenref_var%b2 /= kgenref_var%b2))) THEN
        IF (var%b2 /= var%b2) WRITE(*, *) "localvar%b2 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%b2 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] b2 | IDENTICAL | ", var%b2, " | ", kgenref_var%b2
      ELSE 
          diff_b2 = ABS(var%b2 - kgenref_var%b2) 
          IF (diff_b2 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%b2 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] b2 | IN_TOL | ", var%b2, " | ", kgenref_var%b2, " | ", diff_b2
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%b2 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] b2 | OUT_TOL | ", var%b2, " | ", kgenref_var%b2, " | ", diff_b2
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_b2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_b2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%b5 == kgenref_var%b5) .OR. ((var%b5 /= var%b5) .AND. (kgenref_var%b5 /= kgenref_var%b5))) THEN
        IF (var%b5 /= var%b5) WRITE(*, *) "localvar%b5 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%b5 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] b5 | IDENTICAL | ", var%b5, " | ", kgenref_var%b5
      ELSE 
          diff_b5 = ABS(var%b5 - kgenref_var%b5) 
          IF (diff_b5 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%b5 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] b5 | IN_TOL | ", var%b5, " | ", kgenref_var%b5, " | ", diff_b5
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%b5 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] b5 | OUT_TOL | ", var%b5, " | ", kgenref_var%b5, " | ", diff_b5
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_b5 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_b5 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%a1 == kgenref_var%a1) .OR. ((var%a1 /= var%a1) .AND. (kgenref_var%a1 /= kgenref_var%a1))) THEN
        IF (var%a1 /= var%a1) WRITE(*, *) "localvar%a1 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%a1 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] a1 | IDENTICAL | ", var%a1, " | ", kgenref_var%a1
      ELSE 
          diff_a1 = ABS(var%a1 - kgenref_var%a1) 
          IF (diff_a1 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%a1 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] a1 | IN_TOL | ", var%a1, " | ", kgenref_var%a1, " | ", diff_a1
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%a1 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] a1 | OUT_TOL | ", var%a1, " | ", kgenref_var%a1, " | ", diff_a1
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_a1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_a1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%a2 == kgenref_var%a2) .OR. ((var%a2 /= var%a2) .AND. (kgenref_var%a2 /= kgenref_var%a2))) THEN
        IF (var%a2 /= var%a2) WRITE(*, *) "localvar%a2 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%a2 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] a2 | IDENTICAL | ", var%a2, " | ", kgenref_var%a2
      ELSE 
          diff_a2 = ABS(var%a2 - kgenref_var%a2) 
          IF (diff_a2 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%a2 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] a2 | IN_TOL | ", var%a2, " | ", kgenref_var%a2, " | ", diff_a2
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%a2 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] a2 | OUT_TOL | ", var%a2, " | ", kgenref_var%a2, " | ", diff_a2
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_a2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_a2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%a5 == kgenref_var%a5) .OR. ((var%a5 /= var%a5) .AND. (kgenref_var%a5 /= kgenref_var%a5))) THEN
        IF (var%a5 /= var%a5) WRITE(*, *) "localvar%a5 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%a5 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] a5 | IDENTICAL | ", var%a5, " | ", kgenref_var%a5
      ELSE 
          diff_a5 = ABS(var%a5 - kgenref_var%a5) 
          IF (diff_a5 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%a5 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] a5 | IN_TOL | ", var%a5, " | ", kgenref_var%a5, " | ", diff_a5
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%a5 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] a5 | OUT_TOL | ", var%a5, " | ", kgenref_var%a5, " | ", diff_a5
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_a5 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_a5 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%s1 == kgenref_var%s1) .OR. ((var%s1 /= var%s1) .AND. (kgenref_var%s1 /= kgenref_var%s1))) THEN
        IF (var%s1 /= var%s1) WRITE(*, *) "localvar%s1 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%s1 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] s1 | IDENTICAL | ", var%s1, " | ", kgenref_var%s1
      ELSE 
          diff_s1 = ABS(var%s1 - kgenref_var%s1) 
          IF (diff_s1 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%s1 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] s1 | IN_TOL | ", var%s1, " | ", kgenref_var%s1, " | ", diff_s1
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%s1 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] s1 | OUT_TOL | ", var%s1, " | ", kgenref_var%s1, " | ", diff_s1
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_s1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_s1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%s2 == kgenref_var%s2) .OR. ((var%s2 /= var%s2) .AND. (kgenref_var%s2 /= kgenref_var%s2))) THEN
        IF (var%s2 /= var%s2) WRITE(*, *) "localvar%s2 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%s2 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] s2 | IDENTICAL | ", var%s2, " | ", kgenref_var%s2
      ELSE 
          diff_s2 = ABS(var%s2 - kgenref_var%s2) 
          IF (diff_s2 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%s2 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] s2 | IN_TOL | ", var%s2, " | ", kgenref_var%s2, " | ", diff_s2
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%s2 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] s2 | OUT_TOL | ", var%s2, " | ", kgenref_var%s2, " | ", diff_s2
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_s2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_s2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%s3 == kgenref_var%s3) .OR. ((var%s3 /= var%s3) .AND. (kgenref_var%s3 /= kgenref_var%s3))) THEN
        IF (var%s3 /= var%s3) WRITE(*, *) "localvar%s3 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%s3 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] s3 | IDENTICAL | ", var%s3, " | ", kgenref_var%s3
      ELSE 
          diff_s3 = ABS(var%s3 - kgenref_var%s3) 
          IF (diff_s3 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%s3 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] s3 | IN_TOL | ", var%s3, " | ", kgenref_var%s3, " | ", diff_s3
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%s3 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] s3 | OUT_TOL | ", var%s3, " | ", kgenref_var%s3, " | ", diff_s3
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_s3 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_s3 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%s4 == kgenref_var%s4) .OR. ((var%s4 /= var%s4) .AND. (kgenref_var%s4 /= kgenref_var%s4))) THEN
        IF (var%s4 /= var%s4) WRITE(*, *) "localvar%s4 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%s4 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] s4 | IDENTICAL | ", var%s4, " | ", kgenref_var%s4
      ELSE 
          diff_s4 = ABS(var%s4 - kgenref_var%s4) 
          IF (diff_s4 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%s4 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] s4 | IN_TOL | ", var%s4, " | ", kgenref_var%s4, " | ", diff_s4
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%s4 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] s4 | OUT_TOL | ", var%s4, " | ", kgenref_var%s4, " | ", diff_s4
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_s4 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_s4 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%cn1 == kgenref_var%cn1) .OR. ((var%cn1 /= var%cn1) .AND. (kgenref_var%cn1 /= kgenref_var%cn1))) THEN
        IF (var%cn1 /= var%cn1) WRITE(*, *) "localvar%cn1 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%cn1 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] cn1 | IDENTICAL | ", var%cn1, " | ", kgenref_var%cn1
      ELSE 
          diff_cn1 = ABS(var%cn1 - kgenref_var%cn1) 
          IF (diff_cn1 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cn1 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] cn1 | IN_TOL | ", var%cn1, " | ", kgenref_var%cn1, " | ", diff_cn1
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cn1 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] cn1 | OUT_TOL | ", var%cn1, " | ", kgenref_var%cn1, " | ", diff_cn1
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cn1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cn1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%cn2 == kgenref_var%cn2) .OR. ((var%cn2 /= var%cn2) .AND. (kgenref_var%cn2 /= kgenref_var%cn2))) THEN
        IF (var%cn2 /= var%cn2) WRITE(*, *) "localvar%cn2 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%cn2 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] cn2 | IDENTICAL | ", var%cn2, " | ", kgenref_var%cn2
      ELSE 
          diff_cn2 = ABS(var%cn2 - kgenref_var%cn2) 
          IF (diff_cn2 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cn2 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] cn2 | IN_TOL | ", var%cn2, " | ", kgenref_var%cn2, " | ", diff_cn2
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cn2 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] cn2 | OUT_TOL | ", var%cn2, " | ", kgenref_var%cn2, " | ", diff_cn2
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cn2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cn2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%st_sh == kgenref_var%st_sh) .OR. ((var%st_sh /= var%st_sh) .AND. (kgenref_var%st_sh /= kgenref_var%st_sh))) THEN
        IF (var%st_sh /= var%st_sh) WRITE(*, *) "localvar%st_sh is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%st_sh is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] st_sh | IDENTICAL | ", var%st_sh, " | ", kgenref_var%st_sh
      ELSE 
          diff_st_sh = ABS(var%st_sh - kgenref_var%st_sh) 
          IF (diff_st_sh <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%st_sh is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] st_sh | IN_TOL | ", var%st_sh, " | ", kgenref_var%st_sh, " | ", diff_st_sh
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%st_sh is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] st_sh | OUT_TOL | ", var%st_sh, " | ", kgenref_var%st_sh, " | ", diff_st_sh
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_st_sh 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_st_sh 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%cd0 == kgenref_var%cd0) .OR. ((var%cd0 /= var%cd0) .AND. (kgenref_var%cd0 /= kgenref_var%cd0))) THEN
        IF (var%cd0 /= var%cd0) WRITE(*, *) "localvar%cd0 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%cd0 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] cd0 | IDENTICAL | ", var%cd0, " | ", kgenref_var%cd0
      ELSE 
          diff_cd0 = ABS(var%cd0 - kgenref_var%cd0) 
          IF (diff_cd0 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cd0 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] cd0 | IN_TOL | ", var%cd0, " | ", kgenref_var%cd0, " | ", diff_cd0
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cd0 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] cd0 | OUT_TOL | ", var%cd0, " | ", kgenref_var%cd0, " | ", diff_cd0
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cd0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cd0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%cm0 == kgenref_var%cm0) .OR. ((var%cm0 /= var%cm0) .AND. (kgenref_var%cm0 /= kgenref_var%cm0))) THEN
        IF (var%cm0 /= var%cm0) WRITE(*, *) "localvar%cm0 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%cm0 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] cm0 | IDENTICAL | ", var%cm0, " | ", kgenref_var%cm0
      ELSE 
          diff_cm0 = ABS(var%cm0 - kgenref_var%cm0) 
          IF (diff_cm0 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cm0 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] cm0 | IN_TOL | ", var%cm0, " | ", kgenref_var%cm0, " | ", diff_cm0
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cm0 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] cm0 | OUT_TOL | ", var%cm0, " | ", kgenref_var%cm0, " | ", diff_cm0
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cm0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cm0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%k0 == kgenref_var%k0) .OR. ((var%k0 /= var%k0) .AND. (kgenref_var%k0 /= kgenref_var%k0))) THEN
        IF (var%k0 /= var%k0) WRITE(*, *) "localvar%k0 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%k0 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] k0 | IDENTICAL | ", var%k0, " | ", kgenref_var%k0
      ELSE 
          diff_k0 = ABS(var%k0 - kgenref_var%k0) 
          IF (diff_k0 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k0 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] k0 | IN_TOL | ", var%k0, " | ", kgenref_var%k0, " | ", diff_k0
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k0 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] k0 | OUT_TOL | ", var%k0, " | ", kgenref_var%k0, " | ", diff_k0
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%k1 == kgenref_var%k1) .OR. ((var%k1 /= var%k1) .AND. (kgenref_var%k1 /= kgenref_var%k1))) THEN
        IF (var%k1 /= var%k1) WRITE(*, *) "localvar%k1 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%k1 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] k1 | IDENTICAL | ", var%k1, " | ", kgenref_var%k1
      ELSE 
          diff_k1 = ABS(var%k1 - kgenref_var%k1) 
          IF (diff_k1 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k1 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] k1 | IN_TOL | ", var%k1, " | ", kgenref_var%k1, " | ", diff_k1
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k1 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] k1 | OUT_TOL | ", var%k1, " | ", kgenref_var%k1, " | ", diff_k1
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k1 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%k2 == kgenref_var%k2) .OR. ((var%k2 /= var%k2) .AND. (kgenref_var%k2 /= kgenref_var%k2))) THEN
        IF (var%k2 /= var%k2) WRITE(*, *) "localvar%k2 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%k2 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] k2 | IDENTICAL | ", var%k2, " | ", kgenref_var%k2
      ELSE 
          diff_k2 = ABS(var%k2 - kgenref_var%k2) 
          IF (diff_k2 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k2 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] k2 | IN_TOL | ", var%k2, " | ", kgenref_var%k2, " | ", diff_k2
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k2 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] k2 | OUT_TOL | ", var%k2, " | ", kgenref_var%k2, " | ", diff_k2
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k2 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%k3 == kgenref_var%k3) .OR. ((var%k3 /= var%k3) .AND. (kgenref_var%k3 /= kgenref_var%k3))) THEN
        IF (var%k3 /= var%k3) WRITE(*, *) "localvar%k3 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%k3 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] k3 | IDENTICAL | ", var%k3, " | ", kgenref_var%k3
      ELSE 
          diff_k3 = ABS(var%k3 - kgenref_var%k3) 
          IF (diff_k3 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k3 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] k3 | IN_TOL | ", var%k3, " | ", kgenref_var%k3, " | ", diff_k3
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k3 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] k3 | OUT_TOL | ", var%k3, " | ", kgenref_var%k3, " | ", diff_k3
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k3 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k3 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%k1_hat == kgenref_var%k1_hat) .OR. ((var%k1_hat /= var%k1_hat) .AND. (kgenref_var%k1_hat /= kgenref_var%k1_hat))) THEN
        IF (var%k1_hat /= var%k1_hat) WRITE(*, *) "localvar%k1_hat is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%k1_hat is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] k1_hat | IDENTICAL | ", var%k1_hat, " | ", kgenref_var%k1_hat
      ELSE 
          diff_k1_hat = ABS(var%k1_hat - kgenref_var%k1_hat) 
          IF (diff_k1_hat <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k1_hat is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] k1_hat | IN_TOL | ", var%k1_hat, " | ", kgenref_var%k1_hat, " | ", diff_k1_hat
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k1_hat is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] k1_hat | OUT_TOL | ", var%k1_hat, " | ", kgenref_var%k1_hat, " | ", diff_k1_hat
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k1_hat 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_k1_hat 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%x_cp_bar == kgenref_var%x_cp_bar) .OR. ((var%x_cp_bar /= var%x_cp_bar) .AND. (kgenref_var%x_cp_bar /= kgenref_var%x_cp_bar))) THEN
        IF (var%x_cp_bar /= var%x_cp_bar) WRITE(*, *) "localvar%x_cp_bar is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%x_cp_bar is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] x_cp_bar | IDENTICAL | ", var%x_cp_bar, " | ", kgenref_var%x_cp_bar
      ELSE 
          diff_x_cp_bar = ABS(var%x_cp_bar - kgenref_var%x_cp_bar) 
          IF (diff_x_cp_bar <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%x_cp_bar is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] x_cp_bar | IN_TOL | ", var%x_cp_bar, " | ", kgenref_var%x_cp_bar, " | ", diff_x_cp_bar
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%x_cp_bar is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] x_cp_bar | OUT_TOL | ", var%x_cp_bar, " | ", kgenref_var%x_cp_bar, " | ", diff_x_cp_bar
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_x_cp_bar 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_x_cp_bar 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%uacutout == kgenref_var%uacutout) .OR. ((var%uacutout /= var%uacutout) .AND. (kgenref_var%uacutout /= kgenref_var%uacutout))) THEN
        IF (var%uacutout /= var%uacutout) WRITE(*, *) "localvar%uacutout is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%uacutout is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] uacutout | IDENTICAL | ", var%uacutout, " | ", kgenref_var%uacutout
      ELSE 
          diff_uacutout = ABS(var%uacutout - kgenref_var%uacutout) 
          IF (diff_uacutout <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%uacutout is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] uacutout | IN_TOL | ", var%uacutout, " | ", kgenref_var%uacutout, " | ", diff_uacutout
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%uacutout is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] uacutout | OUT_TOL | ", var%uacutout, " | ", kgenref_var%uacutout, " | ", diff_uacutout
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_uacutout 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_uacutout 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%uacutout_delta == kgenref_var%uacutout_delta) .OR. ((var%uacutout_delta /= var%uacutout_delta) .AND. (kgenref_var%uacutout_delta /= kgenref_var%uacutout_delta))) THEN
        IF (var%uacutout_delta /= var%uacutout_delta) WRITE(*, *) "localvar%uacutout_delta is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%uacutout_delta is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] uacutout_delta | IDENTICAL | ", var%uacutout_delta, " | ", kgenref_var%uacutout_delta
      ELSE 
          diff_uacutout_delta = ABS(var%uacutout_delta - kgenref_var%uacutout_delta) 
          IF (diff_uacutout_delta <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%uacutout_delta is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] uacutout_delta | IN_TOL | ", var%uacutout_delta, " | ", kgenref_var%uacutout_delta, " | ", diff_uacutout_delta
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%uacutout_delta is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] uacutout_delta | OUT_TOL | ", var%uacutout_delta, " | ", kgenref_var%uacutout_delta, " | ", diff_uacutout_delta
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_uacutout_delta 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_uacutout_delta 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%uacutout_blend == kgenref_var%uacutout_blend) .OR. ((var%uacutout_blend /= var%uacutout_blend) .AND. (kgenref_var%uacutout_blend /= kgenref_var%uacutout_blend))) THEN
        IF (var%uacutout_blend /= var%uacutout_blend) WRITE(*, *) "localvar%uacutout_blend is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%uacutout_blend is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] uacutout_blend | IDENTICAL | ", var%uacutout_blend, " | ", kgenref_var%uacutout_blend
      ELSE 
          diff_uacutout_blend = ABS(var%uacutout_blend - kgenref_var%uacutout_blend) 
          IF (diff_uacutout_blend <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%uacutout_blend is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] uacutout_blend | IN_TOL | ", var%uacutout_blend, " | ", kgenref_var%uacutout_blend, " | ", diff_uacutout_blend
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%uacutout_blend is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] uacutout_blend | OUT_TOL | ", var%uacutout_blend, " | ", kgenref_var%uacutout_blend, " | ", diff_uacutout_blend
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_uacutout_blend 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_uacutout_blend 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%filtcutoff == kgenref_var%filtcutoff) .OR. ((var%filtcutoff /= var%filtcutoff) .AND. (kgenref_var%filtcutoff /= kgenref_var%filtcutoff))) THEN
        IF (var%filtcutoff /= var%filtcutoff) WRITE(*, *) "localvar%filtcutoff is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%filtcutoff is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] filtcutoff | IDENTICAL | ", var%filtcutoff, " | ", kgenref_var%filtcutoff
      ELSE 
          diff_filtcutoff = ABS(var%filtcutoff - kgenref_var%filtcutoff) 
          IF (diff_filtcutoff <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%filtcutoff is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] filtcutoff | IN_TOL | ", var%filtcutoff, " | ", kgenref_var%filtcutoff, " | ", diff_filtcutoff
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%filtcutoff is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] filtcutoff | OUT_TOL | ", var%filtcutoff, " | ", kgenref_var%filtcutoff, " | ", diff_filtcutoff
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_filtcutoff 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_filtcutoff 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%alphaupper == kgenref_var%alphaupper) .OR. ((var%alphaupper /= var%alphaupper) .AND. (kgenref_var%alphaupper /= kgenref_var%alphaupper))) THEN
        IF (var%alphaupper /= var%alphaupper) WRITE(*, *) "localvar%alphaupper is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%alphaupper is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] alphaupper | IDENTICAL | ", var%alphaupper, " | ", kgenref_var%alphaupper
      ELSE 
          diff_alphaupper = ABS(var%alphaupper - kgenref_var%alphaupper) 
          IF (diff_alphaupper <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alphaupper is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] alphaupper | IN_TOL | ", var%alphaupper, " | ", kgenref_var%alphaupper, " | ", diff_alphaupper
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alphaupper is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] alphaupper | OUT_TOL | ", var%alphaupper, " | ", kgenref_var%alphaupper, " | ", diff_alphaupper
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alphaupper 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alphaupper 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%alphalower == kgenref_var%alphalower) .OR. ((var%alphalower /= var%alphalower) .AND. (kgenref_var%alphalower /= kgenref_var%alphalower))) THEN
        IF (var%alphalower /= var%alphalower) WRITE(*, *) "localvar%alphalower is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%alphalower is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] alphalower | IDENTICAL | ", var%alphalower, " | ", kgenref_var%alphalower
      ELSE 
          diff_alphalower = ABS(var%alphalower - kgenref_var%alphalower) 
          IF (diff_alphalower <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alphalower is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] alphalower | IN_TOL | ", var%alphalower, " | ", kgenref_var%alphalower, " | ", diff_alphalower
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alphalower is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] alphalower | OUT_TOL | ", var%alphalower, " | ", kgenref_var%alphalower, " | ", diff_alphalower
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alphalower 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alphalower 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%c_alphalower == kgenref_var%c_alphalower) .OR. ((var%c_alphalower /= var%c_alphalower) .AND. (kgenref_var%c_alphalower /= kgenref_var%c_alphalower))) THEN
        IF (var%c_alphalower /= var%c_alphalower) WRITE(*, *) "localvar%c_alphalower is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%c_alphalower is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] c_alphalower | IDENTICAL | ", var%c_alphalower, " | ", kgenref_var%c_alphalower
      ELSE 
          diff_c_alphalower = ABS(var%c_alphalower - kgenref_var%c_alphalower) 
          IF (diff_c_alphalower <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%c_alphalower is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] c_alphalower | IN_TOL | ", var%c_alphalower, " | ", kgenref_var%c_alphalower, " | ", diff_c_alphalower
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%c_alphalower is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] c_alphalower | OUT_TOL | ", var%c_alphalower, " | ", kgenref_var%c_alphalower, " | ", diff_c_alphalower
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_c_alphalower 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_c_alphalower 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%c_alphaupper == kgenref_var%c_alphaupper) .OR. ((var%c_alphaupper /= var%c_alphaupper) .AND. (kgenref_var%c_alphaupper /= kgenref_var%c_alphaupper))) THEN
        IF (var%c_alphaupper /= var%c_alphaupper) WRITE(*, *) "localvar%c_alphaupper is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%c_alphaupper is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] c_alphaupper | IDENTICAL | ", var%c_alphaupper, " | ", kgenref_var%c_alphaupper
      ELSE 
          diff_c_alphaupper = ABS(var%c_alphaupper - kgenref_var%c_alphaupper) 
          IF (diff_c_alphaupper <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%c_alphaupper is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] c_alphaupper | IN_TOL | ", var%c_alphaupper, " | ", kgenref_var%c_alphaupper, " | ", diff_c_alphaupper
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%c_alphaupper is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] c_alphaupper | OUT_TOL | ", var%c_alphaupper, " | ", kgenref_var%c_alphaupper, " | ", diff_c_alphaupper
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_c_alphaupper 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_c_alphaupper 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%alpha0reverseflow == kgenref_var%alpha0reverseflow) .OR. ((var%alpha0reverseflow /= var%alpha0reverseflow) .AND. (kgenref_var%alpha0reverseflow /= kgenref_var%alpha0reverseflow))) THEN
        IF (var%alpha0reverseflow /= var%alpha0reverseflow) WRITE(*, *) "localvar%alpha0reverseflow is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%alpha0reverseflow is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] alpha0reverseflow | IDENTICAL | ", var%alpha0reverseflow, " | ", kgenref_var%alpha0reverseflow
      ELSE 
          diff_alpha0reverseflow = ABS(var%alpha0reverseflow - kgenref_var%alpha0reverseflow) 
          IF (diff_alpha0reverseflow <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha0reverseflow is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] alpha0reverseflow | IN_TOL | ", var%alpha0reverseflow, " | ", kgenref_var%alpha0reverseflow, " | ", diff_alpha0reverseflow
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha0reverseflow is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] alpha0reverseflow | OUT_TOL | ", var%alpha0reverseflow, " | ", kgenref_var%alpha0reverseflow, " | ", diff_alpha0reverseflow
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alpha0reverseflow 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alpha0reverseflow 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%alphabreakupper == kgenref_var%alphabreakupper) .OR. ((var%alphabreakupper /= var%alphabreakupper) .AND. (kgenref_var%alphabreakupper /= kgenref_var%alphabreakupper))) THEN
        IF (var%alphabreakupper /= var%alphabreakupper) WRITE(*, *) "localvar%alphabreakupper is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%alphabreakupper is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] alphabreakupper | IDENTICAL | ", var%alphabreakupper, " | ", kgenref_var%alphabreakupper
      ELSE 
          diff_alphabreakupper = ABS(var%alphabreakupper - kgenref_var%alphabreakupper) 
          IF (diff_alphabreakupper <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alphabreakupper is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] alphabreakupper | IN_TOL | ", var%alphabreakupper, " | ", kgenref_var%alphabreakupper, " | ", diff_alphabreakupper
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alphabreakupper is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] alphabreakupper | OUT_TOL | ", var%alphabreakupper, " | ", kgenref_var%alphabreakupper, " | ", diff_alphabreakupper
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alphabreakupper 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alphabreakupper 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%cnbreakupper == kgenref_var%cnbreakupper) .OR. ((var%cnbreakupper /= var%cnbreakupper) .AND. (kgenref_var%cnbreakupper /= kgenref_var%cnbreakupper))) THEN
        IF (var%cnbreakupper /= var%cnbreakupper) WRITE(*, *) "localvar%cnbreakupper is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%cnbreakupper is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] cnbreakupper | IDENTICAL | ", var%cnbreakupper, " | ", kgenref_var%cnbreakupper
      ELSE 
          diff_cnbreakupper = ABS(var%cnbreakupper - kgenref_var%cnbreakupper) 
          IF (diff_cnbreakupper <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cnbreakupper is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] cnbreakupper | IN_TOL | ", var%cnbreakupper, " | ", kgenref_var%cnbreakupper, " | ", diff_cnbreakupper
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cnbreakupper is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] cnbreakupper | OUT_TOL | ", var%cnbreakupper, " | ", kgenref_var%cnbreakupper, " | ", diff_cnbreakupper
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cnbreakupper 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cnbreakupper 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%alphabreaklower == kgenref_var%alphabreaklower) .OR. ((var%alphabreaklower /= var%alphabreaklower) .AND. (kgenref_var%alphabreaklower /= kgenref_var%alphabreaklower))) THEN
        IF (var%alphabreaklower /= var%alphabreaklower) WRITE(*, *) "localvar%alphabreaklower is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%alphabreaklower is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] alphabreaklower | IDENTICAL | ", var%alphabreaklower, " | ", kgenref_var%alphabreaklower
      ELSE 
          diff_alphabreaklower = ABS(var%alphabreaklower - kgenref_var%alphabreaklower) 
          IF (diff_alphabreaklower <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alphabreaklower is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] alphabreaklower | IN_TOL | ", var%alphabreaklower, " | ", kgenref_var%alphabreaklower, " | ", diff_alphabreaklower
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alphabreaklower is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] alphabreaklower | OUT_TOL | ", var%alphabreaklower, " | ", kgenref_var%alphabreaklower, " | ", diff_alphabreaklower
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alphabreaklower 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_alphabreaklower 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%cnbreaklower == kgenref_var%cnbreaklower) .OR. ((var%cnbreaklower /= var%cnbreaklower) .AND. (kgenref_var%cnbreaklower /= kgenref_var%cnbreaklower))) THEN
        IF (var%cnbreaklower /= var%cnbreaklower) WRITE(*, *) "localvar%cnbreaklower is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%cnbreaklower is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] cnbreaklower | IDENTICAL | ", var%cnbreaklower, " | ", kgenref_var%cnbreaklower
      ELSE 
          diff_cnbreaklower = ABS(var%cnbreaklower - kgenref_var%cnbreaklower) 
          IF (diff_cnbreaklower <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cnbreaklower is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] cnbreaklower | IN_TOL | ", var%cnbreaklower, " | ", kgenref_var%cnbreaklower, " | ", diff_cnbreaklower
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cnbreaklower is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] cnbreaklower | OUT_TOL | ", var%cnbreaklower, " | ", kgenref_var%cnbreaklower, " | ", diff_cnbreaklower
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cnbreaklower 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_cnbreaklower 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (dtype_check_status%numTotal == dtype_check_status%numIdentical) THEN 
          check_status%numIdentical = check_status%numIdentical + 1 
      ELSE IF (dtype_check_status%numOutTol > 0) THEN 
          check_status%numOutTol = check_status%numOutTol + 1 
      ELSE IF (dtype_check_status%numInTol > 0) THEN 
          check_status%numInTol = check_status%numInTol + 1 
      END IF   
  END SUBROUTINE kv_airfoilinfo_types_afi_ua_bl_type 
    
  !verify state subroutine for kv_airfoilinfo_types_afi_table_type 
  RECURSIVE SUBROUTINE kv_airfoilinfo_types_afi_table_type(varname, check_status, var, kgenref_var) 
      CHARACTER(LEN=*), INTENT(IN) :: varname 
      TYPE(check_t), INTENT(INOUT) :: check_status 
      TYPE(afi_table_type), INTENT(IN) :: var, kgenref_var 
      TYPE(check_t) :: dtype_check_status, comp_check_status 
      INTEGER :: check_result 
      LOGICAL :: is_print = .FALSE. 
        
      INTEGER :: n_alpha 
      real(KIND=reki) :: nrmsdiff_alpha, rmsdiff_alpha 
      real(KIND=reki), ALLOCATABLE :: buf1_alpha(:), buf2_alpha(:) 
      INTEGER :: n_coefs 
      real(KIND=reki) :: nrmsdiff_coefs, rmsdiff_coefs 
      real(KIND=reki), ALLOCATABLE :: buf1_coefs(:,:), buf2_coefs(:,:) 
      INTEGER :: n_splinecoefs 
      real(KIND=reki) :: nrmsdiff_splinecoefs, rmsdiff_splinecoefs 
      real(KIND=reki), ALLOCATABLE :: buf1_splinecoefs(:,:,:), buf2_splinecoefs(:,:,:) 
      real(KIND=reki) :: diff_userprop 
      real(KIND=reki) :: diff_re 
      integer(KIND=intki) :: diff_numalf 
        
      check_status%numTotal = check_status%numTotal + 1 
        
      CALL kgen_init_check(dtype_check_status, rank=check_status%rank) 
      IF (ALLOCATED(var%alpha)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%alpha == kgenref_var%alpha)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%alpha is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
              WRITE(*, *) "[VIT_ARRAY] alpha | IDENTICAL | size=", SIZE(var%alpha)
          ELSE 
              ALLOCATE (buf1_alpha(SIZE(var%alpha,dim=1))) 
              ALLOCATE (buf2_alpha(SIZE(var%alpha,dim=1))) 
              n_alpha = COUNT(var%alpha /= kgenref_var%alpha) 
              WHERE ( ABS(kgenref_var%alpha) > kgen_minvalue ) 
                  buf1_alpha = ((var%alpha-kgenref_var%alpha)/kgenref_var%alpha)**2 
                  buf2_alpha = (var%alpha-kgenref_var%alpha)**2 
              ELSEWHERE 
                  buf1_alpha = (var%alpha-kgenref_var%alpha)**2 
                  buf2_alpha = buf1_alpha 
              END WHERE   
              nrmsdiff_alpha = SQRT(SUM(buf1_alpha)/REAL(n_alpha)) 
              rmsdiff_alpha = SQRT(SUM(buf2_alpha)/REAL(n_alpha)) 
              IF (rmsdiff_alpha > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%alpha is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
                  WRITE(*, *) "[VIT_ARRAY] alpha | OUT_TOL | n_diff=", n_alpha, " | rms=", rmsdiff_alpha
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%alpha is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
                  WRITE(*, *) "[VIT_ARRAY] alpha | IN_TOL | n_diff=", n_alpha, " | rms=", rmsdiff_alpha
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%alpha /= kgenref_var%alpha), " of ", size( var%alpha ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%alpha)/real(size(var%alpha)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%alpha)/real(size(kgenref_var%alpha)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_alpha 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_alpha 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%alpha /= kgenref_var%alpha), " of ", size( var%alpha ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%alpha)/real(size(var%alpha)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%alpha)/real(size(kgenref_var%alpha)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_alpha 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_alpha 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%coefs)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%coefs == kgenref_var%coefs)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%coefs is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
              WRITE(*, *) "[VIT_ARRAY] coefs | IDENTICAL | size=", SIZE(var%coefs)
          ELSE 
              ALLOCATE (buf1_coefs(SIZE(var%coefs,dim=1),SIZE(var%coefs,dim=2))) 
              ALLOCATE (buf2_coefs(SIZE(var%coefs,dim=1),SIZE(var%coefs,dim=2))) 
              n_coefs = COUNT(var%coefs /= kgenref_var%coefs) 
              WHERE ( ABS(kgenref_var%coefs) > kgen_minvalue ) 
                  buf1_coefs = ((var%coefs-kgenref_var%coefs)/kgenref_var%coefs)**2 
                  buf2_coefs = (var%coefs-kgenref_var%coefs)**2 
              ELSEWHERE 
                  buf1_coefs = (var%coefs-kgenref_var%coefs)**2 
                  buf2_coefs = buf1_coefs 
              END WHERE   
              nrmsdiff_coefs = SQRT(SUM(buf1_coefs)/REAL(n_coefs)) 
              rmsdiff_coefs = SQRT(SUM(buf2_coefs)/REAL(n_coefs)) 
              IF (rmsdiff_coefs > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%coefs is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
                  WRITE(*, *) "[VIT_ARRAY] coefs | OUT_TOL | n_diff=", n_coefs, " | rms=", rmsdiff_coefs
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%coefs is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
                  WRITE(*, *) "[VIT_ARRAY] coefs | IN_TOL | n_diff=", n_coefs, " | rms=", rmsdiff_coefs
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%coefs /= kgenref_var%coefs), " of ", size( var%coefs ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%coefs)/real(size(var%coefs)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%coefs)/real(size(kgenref_var%coefs)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_coefs 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_coefs 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%coefs /= kgenref_var%coefs), " of ", size( var%coefs ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%coefs)/real(size(var%coefs)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%coefs)/real(size(kgenref_var%coefs)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_coefs 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_coefs 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%splinecoefs)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%splinecoefs == kgenref_var%splinecoefs)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%splinecoefs is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
              WRITE(*, *) "[VIT_ARRAY] splinecoefs | IDENTICAL | size=", SIZE(var%splinecoefs)
          ELSE 
              ALLOCATE (buf1_splinecoefs(SIZE(var%splinecoefs,dim=1),SIZE(var%splinecoefs,dim=2),SIZE(var%splinecoefs,dim=3))) 
              ALLOCATE (buf2_splinecoefs(SIZE(var%splinecoefs,dim=1),SIZE(var%splinecoefs,dim=2),SIZE(var%splinecoefs,dim=3))) 
              n_splinecoefs = COUNT(var%splinecoefs /= kgenref_var%splinecoefs) 
              WHERE ( ABS(kgenref_var%splinecoefs) > kgen_minvalue ) 
                  buf1_splinecoefs = ((var%splinecoefs-kgenref_var%splinecoefs)/kgenref_var%splinecoefs)**2 
                  buf2_splinecoefs = (var%splinecoefs-kgenref_var%splinecoefs)**2 
              ELSEWHERE 
                  buf1_splinecoefs = (var%splinecoefs-kgenref_var%splinecoefs)**2 
                  buf2_splinecoefs = buf1_splinecoefs 
              END WHERE   
              nrmsdiff_splinecoefs = SQRT(SUM(buf1_splinecoefs)/REAL(n_splinecoefs)) 
              rmsdiff_splinecoefs = SQRT(SUM(buf2_splinecoefs)/REAL(n_splinecoefs)) 
              IF (rmsdiff_splinecoefs > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%splinecoefs is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
                  WRITE(*, *) "[VIT_ARRAY] splinecoefs | OUT_TOL | n_diff=", n_splinecoefs, " | rms=", rmsdiff_splinecoefs
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%splinecoefs is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
                  WRITE(*, *) "[VIT_ARRAY] splinecoefs | IN_TOL | n_diff=", n_splinecoefs, " | rms=", rmsdiff_splinecoefs
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%splinecoefs /= kgenref_var%splinecoefs), " of ", size( var%splinecoefs ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%splinecoefs)/real(size(var%splinecoefs)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%splinecoefs)/real(size(kgenref_var%splinecoefs)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_splinecoefs 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_splinecoefs 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%splinecoefs /= kgenref_var%splinecoefs), " of ", size( var%splinecoefs ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%splinecoefs)/real(size(var%splinecoefs)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%splinecoefs)/real(size(kgenref_var%splinecoefs)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_splinecoefs 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_splinecoefs 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%userprop == kgenref_var%userprop) .OR. ((var%userprop /= var%userprop) .AND. (kgenref_var%userprop /= kgenref_var%userprop))) THEN
        IF (var%userprop /= var%userprop) WRITE(*, *) "localvar%userprop is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%userprop is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] userprop | IDENTICAL | ", var%userprop, " | ", kgenref_var%userprop
      ELSE 
          diff_userprop = ABS(var%userprop - kgenref_var%userprop) 
          IF (diff_userprop <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%userprop is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] userprop | IN_TOL | ", var%userprop, " | ", kgenref_var%userprop, " | ", diff_userprop
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%userprop is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] userprop | OUT_TOL | ", var%userprop, " | ", kgenref_var%userprop, " | ", diff_userprop
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_userprop 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_userprop 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%re == kgenref_var%re) .OR. ((var%re /= var%re) .AND. (kgenref_var%re /= kgenref_var%re))) THEN
        IF (var%re /= var%re) WRITE(*, *) "localvar%re is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%re is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] re | IDENTICAL | ", var%re, " | ", kgenref_var%re
      ELSE 
          diff_re = ABS(var%re - kgenref_var%re) 
          IF (diff_re <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%re is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] re | IN_TOL | ", var%re, " | ", kgenref_var%re, " | ", diff_re
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%re is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] re | OUT_TOL | ", var%re, " | ", kgenref_var%re, " | ", diff_re
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_re 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_re 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%numalf == kgenref_var%numalf) .OR. ((var%numalf /= var%numalf) .AND. (kgenref_var%numalf /= kgenref_var%numalf))) THEN
        IF (var%numalf /= var%numalf) WRITE(*, *) "localvar%numalf is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%numalf is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] numalf | IDENTICAL | ", var%numalf, " | ", kgenref_var%numalf
      ELSE 
          diff_numalf = ABS(var%numalf - kgenref_var%numalf) 
          IF (diff_numalf <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%numalf is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
              WRITE(*, *) "[VIT_FIELD] numalf | IN_TOL | ", var%numalf, " | ", kgenref_var%numalf, " | ", diff_numalf
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%numalf is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
              WRITE(*, *) "[VIT_FIELD] numalf | OUT_TOL | ", var%numalf, " | ", kgenref_var%numalf, " | ", diff_numalf
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_numalf 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_numalf 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%constdata .EQV. kgenref_var%constdata) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%constdata is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] numalf | IDENTICAL | ", var%numalf, " | ", kgenref_var%numalf
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%constdata is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
          WRITE(*, *) "[VIT_FIELD] numalf | OUT_TOL | ", var%numalf, " | ", kgenref_var%numalf, " | ", diff_numalf
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%incluadata .EQV. kgenref_var%incluadata) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%incluadata is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] numalf | IDENTICAL | ", var%numalf, " | ", kgenref_var%numalf
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%incluadata is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
          WRITE(*, *) "[VIT_FIELD] numalf | OUT_TOL | ", var%numalf, " | ", kgenref_var%numalf, " | ", diff_numalf
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      CALL kgen_init_check(comp_check_status, rank=check_status%rank) 
      CALL kv_airfoilinfo_types_afi_ua_bl_type("ua_bl", comp_check_status, var%ua_bl, kgenref_var%ua_bl) 
      IF (comp_check_status%numTotal == comp_check_status%numIdentical) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname))//"%ua_bl", " is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
          WRITE(*, *) "[VIT_FIELD] numalf | IDENTICAL | ", var%numalf, " | ", kgenref_var%numalf
      ELSE IF (comp_check_status%numOutTol > 0) THEN 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%ua_bl is NOT IDENTICAL(out of tolerance)." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
          WRITE(*, *) "[VIT_FIELD] numalf | OUT_TOL | ", var%numalf, " | ", kgenref_var%numalf, " | ", diff_numalf
      ELSE IF (comp_check_status%numInTol > 0) THEN 
          dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%ua_bl is NOT IDENTICAL(within tolerance)." 
              END IF   
          END IF   
          check_result = CHECK_IN_TOL 
          WRITE(*, *) "[VIT_FIELD] numalf | IN_TOL | ", var%numalf, " | ", kgenref_var%numalf, " | ", diff_numalf
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "       number of components             : ", comp_check_status%numtotal 
                  WRITE (*, *) "       identical                                   : ", comp_check_status%numidentical 
                  WRITE (*, *) "       not identical - out of tol.: ", comp_check_status%numouttol 
                  WRITE (*, *) "       not identical - within tol.: ", comp_check_status%numintol 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "       number of components             : ", comp_check_status%numtotal 
                  WRITE (*, *) "       identical                                   : ", comp_check_status%numidentical 
                  WRITE (*, *) "       not identical - out of tol.: ", comp_check_status%numouttol 
                  WRITE (*, *) "       not identical - within tol.: ", comp_check_status%numintol 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (dtype_check_status%numTotal == dtype_check_status%numIdentical) THEN 
          check_status%numIdentical = check_status%numIdentical + 1 
      ELSE IF (dtype_check_status%numOutTol > 0) THEN 
          check_status%numOutTol = check_status%numOutTol + 1 
      ELSE IF (dtype_check_status%numInTol > 0) THEN 
          check_status%numInTol = check_status%numInTol + 1 
      END IF   
  END SUBROUTINE kv_airfoilinfo_types_afi_table_type 
    
END MODULE AirfoilInfo_Types
!ENDOFREGISTRYGENERATEDFILE
