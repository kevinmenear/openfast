!KGEN-generated Fortran source file 
  
!Generated at : 2026-04-26 16:01:24 
!KGEN version : 0.8.1 
  
!STARTOFREGISTRYGENERATEDFILE 'BEMT_Types.f90'
! WARNING This file is generated automatically by the FAST registry.
! Do not edit.  Your changes to this file will be lost.
! FAST Registry
!*********************************************************************************************************************************
! BEMT_Types
!.................................................................................................................................
! This file is part of BEMT.
! Copyright (C) 2012-2016 National Renewable Energy Laboratory
! Licensed under the Apache License, Version 2.0 (the "License");
! you may not use this file except in compliance with the License.
! You may obtain a copy of the License at
!     http://www.apache.org/licenses/LICENSE-2.0
! Unless required by applicable law or agreed to in writing, software
! distributed under the License is distributed on an "AS IS" BASIS,
! WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
! See the License for the specific language governing permissions and
! limitations under the License.
! W A R N I N G : This file was automatically generated from the FAST registry.  Changes made to this file may be lost.
!*********************************************************************************************************************************
!> This module contains the user-defined types needed in BEMT. It also contains copy, destroy, pack, and
!! unpack routines associated with each defined data type. This code is automatically generated by the FAST Registry.


!
!
!
!
!
!
!
!
!
MODULE BEMT_Types
!---------------------------------------------------------------------------------------------------------------------------------
    USE unsteadyaero_types 
    USE dbemt_types 
    USE kgen_utils_mod
    USE tprof_mod, ONLY: tstart, tstop, tnull, tprnt 
    USE kgen_utils_mod
    IMPLICIT NONE 
! =========  BEMT_InitInputType  =======

! =======================
! =========  BEMT_InitOutputType  =======

! =======================
! =========  BEMT_SkewWake_InputType  =======

! =======================
! =========  BEMT_ContinuousStateType  =======

! =======================
! =========  BEMT_DiscreteStateType  =======

! =======================
! =========  BEMT_ConstraintStateType  =======

! =======================
! =========  BEMT_OtherStateType  =======

! =======================
! =========  BEMT_MiscVarType  =======

! =======================
! =========  BEMT_ParameterType  =======
  TYPE, PUBLIC :: BEMT_ParameterType
    REAL(DbKi)  :: DT = 0.0_R8Ki      !< time step [s]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: chord      !< Chord length at node [m]
    INTEGER(IntKi)  :: numBlades = 0_IntKi      !< Number of blades [-]
    REAL(ReKi)  :: airDens = 0.0_ReKi      !< Air density [kg/m^3]
    REAL(ReKi)  :: kinVisc = 0.0_ReKi      !< Kinematic air viscosity [m^2/s]
    INTEGER(IntKi)  :: skewWakeMod = 0_IntKi      !< Type of skewed-wake correction model [switch] {0=None, 1=Glauert/Pitt/Peters} [-]
    INTEGER(IntKi)  :: skewRedistrMod = 0_IntKi      !< Type of skewed-wake redistribution model (switch) {0=no redistribution, 1=Glauert/Pitt/Peters, 2=Vortex Cylinder} [unsed only when SkewMod=1] [-]
    REAL(ReKi)  :: aTol = 0.0_ReKi      !< Tolerance for the induction solution [-]
    LOGICAL  :: useTipLoss = .false.      !< Use the Prandtl tip-loss model?  [flag] [-]
    LOGICAL  :: useHubLoss = .false.      !< Use the Prandtl hub-loss model?  [flag] [-]
    LOGICAL  :: useInduction = .false.      !< Include induction in BEMT calculations [flag] { If FALSE then useTanInd will be set to FALSE} [-]
    LOGICAL  :: useTanInd = .false.      !< Include tangential induction in BEMT calculations [flag] [-]
    LOGICAL  :: useAIDrag = .false.      !< Include the drag term in the axial-induction calculation?  [flag] [-]
    LOGICAL  :: useTIDrag = .false.      !< Include the drag term in the tangential-induction calculation?  Ignored if TanInd is False.  [flag] [-]
    INTEGER(IntKi)  :: numBladeNodes = 0_IntKi      !< Number of blade nodes used in the analysis [-]
    INTEGER(IntKi)  :: numReIterations = 0_IntKi      !< Number of iterations for finding the Reynolds number [-]
    INTEGER(IntKi)  :: maxIndIterations = 0_IntKi      !< Maximum number of iterations of induction factor solve [-]
    INTEGER(IntKi) , DIMENSION(:,:), ALLOCATABLE  :: AFindx      !< Index of airfoil data file for blade node location [array of numBladeNodes] [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: tipLossConst      !< A constant computed during initialization based on B*(zTip-zLocal)/(2*zLocal) [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: hubLossConst      !< A constant computed during initialization based on B*(zLocal-zHub)/(2*zHub) [-]
    REAL(ReKi) , DIMENSION(:), ALLOCATABLE  :: zHub      !< Distance to hub for each blade [m]
    TYPE(UA_ParameterType)  :: UA      !< parameters for UnsteadyAero [-]
    TYPE(DBEMT_ParameterType)  :: DBEMT      !< parameters for DBEMT [-]
    LOGICAL  :: UA_Flag = .false.      !< logical flag indicating whether to use UnsteadyAero [-]
    INTEGER(IntKi)  :: DBEMT_Mod = 0_IntKi      !< DBEMT Model.  0 = constant tau1, 1 = time dependent tau1 [-]
    REAL(ReKi)  :: yawCorrFactor = 0.0_ReKi      !< constant used in Pitt/Peters skewed wake model (default is 15*pi/32) [-]
    LOGICAL , DIMENSION(:,:), ALLOCATABLE  :: FixedInductions      !< flag to determine if BEM inductions should be fixed and not modified by dbemt or skewed wake [-]
    LOGICAL  :: MomentumCorr = .false.      !< Momentum Correction {0=Axial Theory, 1 = Glauert Momentum Theory} [-]
    REAL(ReKi)  :: rTipFixMax = 0.0_ReKi      !< Nominally the coned rotor diameter (without prebend), used to align with Bladed calculations [m]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: IntegrateWeight      !< A weighting factor for calculating rotor-averaged values (e.g., AxInd) [-]
    INTEGER(IntKi)  :: lin_nx = 0      !< Number of continuous states for linearization [-]
    INTEGER(IntKi)  :: BEM_Mod = 0_IntKi      !< BEM Model 0=OpenFAST 2=Envision  [-]
  END TYPE BEMT_ParameterType
! =======================
! =========  BEMT_InputType  =======
  TYPE, PUBLIC :: BEMT_InputType
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: theta      !< Twist angle (includes all sources of twist)  [Array of size (NumBlNds,numBlades)] [rad]
    REAL(ReKi)  :: chi0 = 0.0_ReKi      !< Angle between the vector normal to the rotor plane and the wind vector (e.g., the yaw angle in the case of no tilt) [rad]
    REAL(ReKi)  :: psiSkewOffset = 0.0_ReKi      !< Skew azimuth angle offset (relative to 90 deg) of the most downwind blade when chi0 is non-zero [rad]
    REAL(ReKi) , DIMENSION(:), ALLOCATABLE  :: psi_s      !< Skew azimuth angle [rad]
    REAL(ReKi)  :: omega = 0.0_ReKi      !< Angular velocity of rotor [rad/s]
    REAL(ReKi)  :: TSR = 0.0_ReKi      !< Tip-speed ratio (to check if BEM should be turned off) [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Vx      !< Local axial velocity at node [m/s]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Vy      !< Local tangential velocity at node [m/s]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Vz      !< Local radial velocity at node [m/s]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: omega_z      !< rotation of no-sweep-pitch-twist coordinate system around z (for CDBEMT and CUA) [rad/s]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: xVelCorr      !< projection of velocity when yawed + prebend [m/s]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: rLocal      !< Radial distance from center-of-rotation to node [m]
    REAL(ReKi)  :: Un_disk = 0.0_ReKi      !< disk-averaged velocity normal to the rotor disk (for input to DBEMT) [m/s]
    REAL(ReKi) , DIMENSION(1:3)  :: V0 = 0.0_ReKi      !< disk-averaged velocity (for input to SkewWake) [m/s]
    REAL(R8Ki) , DIMENSION(1:3)  :: x_hat_disk = 0.0_R8Ki      !< Hub Orientation vector: normal to rotor disk [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: UserProp      !< Optional user property for interpolating airfoils (per element per blade) [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: CantAngle      !< Cant angle [Array of size (NumBlNds,numBlades)] [rad]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: drdz      !< dr/dz geometric parameter [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: toeAngle      !< Toe angle [Array of size (NumBlNds,numBlades)] [rad]
  END TYPE BEMT_InputType
! =======================
! =========  BEMT_OutputType  =======
  TYPE, PUBLIC :: BEMT_OutputType
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Vrel      !< Total local relative velocity [m/s]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: phi      !< angle between the plane of rotation and the direction of the local wind [rad]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: axInduction      !< axial induction [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: tanInduction      !< tangential induction [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: axInduction_qs      !< axial induction quasi steady [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: tanInduction_qs      !< tangential induction quasi steady [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: k      !< Factor k in blade element theory thrust coefficient [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: k_p      !< Factor kp in blade element theory torque coefficient [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: F      !< Tip/hub loss factor [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Re      !< Reynold's number [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: AOA      !< angle of attack [rad]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cx      !< normal force coefficient (normal to the plane, not chord) of the jth node in the kth blade [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cy      !< tangential force coefficient (tangential to the plane, not chord) of the jth node in the kth blade [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cz      !< axial force coefficient (tangential to the plane, not chord) of the jth node in the kth blade [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cmx      !< pitching moment coefficient (x-component) of the jth node in the kth blade [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cmy      !< pitching moment coefficient (y-component) of the jth node in the kth blade [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cmz      !< pitching moment coefficient (z-component) of the jth node in the kth blade [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cm      !< pitching moment coefficient of the jth node in the kth blade [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cl      !< lift coefficient [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cd      !< drag coefficient [-]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: chi      !< wake skew angle [rad]
    REAL(ReKi) , DIMENSION(:,:), ALLOCATABLE  :: Cpmin      !< min Cpressure [-]
  END TYPE BEMT_OutputType
! =======================
  PUBLIC kr_bemt_types_bemt_parametertype 
  PUBLIC kr_bemt_types_bemt_inputtype 
  PUBLIC kr_bemt_types_bemt_outputtype 
  PUBLIC kv_bemt_types_bemt_parametertype 
  PUBLIC kv_bemt_types_bemt_inputtype 
  PUBLIC kv_bemt_types_bemt_outputtype 

    
  CONTAINS 
    


  !read state subroutine for kr_bemt_types_bemt_parametertype 
  RECURSIVE SUBROUTINE kr_bemt_types_bemt_parametertype(var, kgen_unit, printname, printvar) 
      TYPE(bemt_parametertype), INTENT(INOUT) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
        
      READ (UNIT = kgen_unit) var%dt 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%dt = ", var%dt 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_parametertype_real__reki_dim2(var%chord, kgen_unit, printname // "%chord", .TRUE.) 
      ELSE 
          CALL kr_bemt_parametertype_real__reki_dim2(var%chord, kgen_unit, printname // "%chord", .FALSE.) 
      END IF   
        
      READ (UNIT = kgen_unit) var%numblades 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%numblades = ", var%numblades 
      END IF   
        
      READ (UNIT = kgen_unit) var%airdens 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%airdens = ", var%airdens 
      END IF   
        
      READ (UNIT = kgen_unit) var%kinvisc 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%kinvisc = ", var%kinvisc 
      END IF   
        
      READ (UNIT = kgen_unit) var%skewwakemod 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%skewwakemod = ", var%skewwakemod 
      END IF   
        
      READ (UNIT = kgen_unit) var%skewredistrmod 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%skewredistrmod = ", var%skewredistrmod 
      END IF   
        
      READ (UNIT = kgen_unit) var%atol 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%atol = ", var%atol 
      END IF   
        
      READ (UNIT = kgen_unit) var%usetiploss 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%usetiploss = ", var%usetiploss 
      END IF   
        
      READ (UNIT = kgen_unit) var%usehubloss 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%usehubloss = ", var%usehubloss 
      END IF   
        
      READ (UNIT = kgen_unit) var%useinduction 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%useinduction = ", var%useinduction 
      END IF   
        
      READ (UNIT = kgen_unit) var%usetanind 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%usetanind = ", var%usetanind 
      END IF   
        
      READ (UNIT = kgen_unit) var%useaidrag 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%useaidrag = ", var%useaidrag 
      END IF   
        
      READ (UNIT = kgen_unit) var%usetidrag 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%usetidrag = ", var%usetidrag 
      END IF   
        
      READ (UNIT = kgen_unit) var%numbladenodes 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%numbladenodes = ", var%numbladenodes 
      END IF   
        
      READ (UNIT = kgen_unit) var%numreiterations 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%numreiterations = ", var%numreiterations 
      END IF   
        
      READ (UNIT = kgen_unit) var%maxinditerations 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%maxinditerations = ", var%maxinditerations 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_parametertype_integer__intki_dim2(var%afindx, kgen_unit, printname // "%afindx", .TRUE.) 
      ELSE 
          CALL kr_bemt_parametertype_integer__intki_dim2(var%afindx, kgen_unit, printname // "%afindx", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_parametertype_real__reki_dim2(var%tiplossconst, kgen_unit, printname // "%tiplossconst", .TRUE.) 
      ELSE 
          CALL kr_bemt_parametertype_real__reki_dim2(var%tiplossconst, kgen_unit, printname // "%tiplossconst", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_parametertype_real__reki_dim2(var%hublossconst, kgen_unit, printname // "%hublossconst", .TRUE.) 
      ELSE 
          CALL kr_bemt_parametertype_real__reki_dim2(var%hublossconst, kgen_unit, printname // "%hublossconst", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_parametertype_real__reki_dim1(var%zhub, kgen_unit, printname // "%zhub", .TRUE.) 
      ELSE 
          CALL kr_bemt_parametertype_real__reki_dim1(var%zhub, kgen_unit, printname // "%zhub", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_unsteadyaero_types_ua_parametertype(var%ua, kgen_unit, printname // "%ua", .TRUE.) 
      ELSE 
          CALL kr_unsteadyaero_types_ua_parametertype(var%ua, kgen_unit, printname // "%ua", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_dbemt_types_dbemt_parametertype(var%dbemt, kgen_unit, printname // "%dbemt", .TRUE.) 
      ELSE 
          CALL kr_dbemt_types_dbemt_parametertype(var%dbemt, kgen_unit, printname // "%dbemt", .FALSE.) 
      END IF   
        
      READ (UNIT = kgen_unit) var%ua_flag 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%ua_flag = ", var%ua_flag 
      END IF   
        
      READ (UNIT = kgen_unit) var%dbemt_mod 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%dbemt_mod = ", var%dbemt_mod 
      END IF   
        
      READ (UNIT = kgen_unit) var%yawcorrfactor 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%yawcorrfactor = ", var%yawcorrfactor 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_parametertype_logical___dim2(var%fixedinductions, kgen_unit, printname // "%fixedinductions", .TRUE.) 
      ELSE 
          CALL kr_bemt_parametertype_logical___dim2(var%fixedinductions, kgen_unit, printname // "%fixedinductions", .FALSE.) 
      END IF   
        
      READ (UNIT = kgen_unit) var%momentumcorr 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%momentumcorr = ", var%momentumcorr 
      END IF   
        
      READ (UNIT = kgen_unit) var%rtipfixmax 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%rtipfixmax = ", var%rtipfixmax 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_parametertype_real__reki_dim2(var%integrateweight, kgen_unit, printname // "%integrateweight", .TRUE.) 
      ELSE 
          CALL kr_bemt_parametertype_real__reki_dim2(var%integrateweight, kgen_unit, printname // "%integrateweight", .FALSE.) 
      END IF   
        
      READ (UNIT = kgen_unit) var%lin_nx 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%lin_nx = ", var%lin_nx 
      END IF   
        
      READ (UNIT = kgen_unit) var%bem_mod 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%bem_mod = ", var%bem_mod 
      END IF   
        
  END SUBROUTINE kr_bemt_types_bemt_parametertype 
    
  !write state subroutine for kr_bemt_parametertype_real__reki_dim2 
  SUBROUTINE kr_bemt_parametertype_real__reki_dim2(var, kgen_unit, printname, printvar) 
      REAL(KIND=reki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:,:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1, idx2 
      INTEGER, DIMENSION(2,2) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          READ (UNIT = kgen_unit) kgen_bound(1, 2) 
          READ (UNIT = kgen_unit) kgen_bound(2, 2) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1), kgen_bound(1,2):kgen_bound(2,2))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_bemt_parametertype_real__reki_dim2 
    
  !write state subroutine for kr_bemt_parametertype_integer__intki_dim2 
  SUBROUTINE kr_bemt_parametertype_integer__intki_dim2(var, kgen_unit, printname, printvar) 
      INTEGER(KIND=intki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:,:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1, idx2 
      INTEGER, DIMENSION(2,2) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          READ (UNIT = kgen_unit) kgen_bound(1, 2) 
          READ (UNIT = kgen_unit) kgen_bound(2, 2) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1), kgen_bound(1,2):kgen_bound(2,2))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_bemt_parametertype_integer__intki_dim2 
    
  !write state subroutine for kr_bemt_parametertype_real__reki_dim1 
  SUBROUTINE kr_bemt_parametertype_real__reki_dim1(var, kgen_unit, printname, printvar) 
      REAL(KIND=reki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1 
      INTEGER, DIMENSION(2,1) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_bemt_parametertype_real__reki_dim1 
    
  !write state subroutine for kr_bemt_parametertype_logical___dim2 
  SUBROUTINE kr_bemt_parametertype_logical___dim2(var, kgen_unit, printname, printvar) 
      LOGICAL, INTENT(INOUT), ALLOCATABLE, DIMENSION(:,:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1, idx2 
      INTEGER, DIMENSION(2,2) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          READ (UNIT = kgen_unit) kgen_bound(1, 2) 
          READ (UNIT = kgen_unit) kgen_bound(2, 2) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1), kgen_bound(1,2):kgen_bound(2,2))) 
          READ (UNIT = kgen_unit) var 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: " // printname // " = ", var 
          END IF   
      END IF   
        
  END SUBROUTINE kr_bemt_parametertype_logical___dim2 
    
  !read state subroutine for kr_bemt_types_bemt_inputtype 
  RECURSIVE SUBROUTINE kr_bemt_types_bemt_inputtype(var, kgen_unit, printname, printvar) 
      TYPE(bemt_inputtype), INTENT(INOUT) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%theta, kgen_unit, printname // "%theta", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%theta, kgen_unit, printname // "%theta", .FALSE.) 
      END IF   
        
      READ (UNIT = kgen_unit) var%chi0 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%chi0 = ", var%chi0 
      END IF   
        
      READ (UNIT = kgen_unit) var%psiskewoffset 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%psiskewoffset = ", var%psiskewoffset 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim1(var%psi_s, kgen_unit, printname // "%psi_s", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim1(var%psi_s, kgen_unit, printname // "%psi_s", .FALSE.) 
      END IF   
        
      READ (UNIT = kgen_unit) var%omega 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%omega = ", var%omega 
      END IF   
        
      READ (UNIT = kgen_unit) var%tsr 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%tsr = ", var%tsr 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%vx, kgen_unit, printname // "%vx", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%vx, kgen_unit, printname // "%vx", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%vy, kgen_unit, printname // "%vy", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%vy, kgen_unit, printname // "%vy", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%vz, kgen_unit, printname // "%vz", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%vz, kgen_unit, printname // "%vz", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%omega_z, kgen_unit, printname // "%omega_z", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%omega_z, kgen_unit, printname // "%omega_z", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%xvelcorr, kgen_unit, printname // "%xvelcorr", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%xvelcorr, kgen_unit, printname // "%xvelcorr", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%rlocal, kgen_unit, printname // "%rlocal", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%rlocal, kgen_unit, printname // "%rlocal", .FALSE.) 
      END IF   
        
      READ (UNIT = kgen_unit) var%un_disk 
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          WRITE (*, *) "KGEN DEBUG: " // printname // "%un_disk = ", var%un_disk 
      END IF   
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) var%v0 
          CALL kgen_array_sumcheck(printname // "%v0", kgen_array_sum, DBLE(SUM(var%v0, mask=(var%v0 .eq. var%v0))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // "%v0)) = ", DBLE(SUM(var%v0, mask=(var%v0 .eq. var%v0))) 
          END IF   
      END IF   
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) var%x_hat_disk 
          CALL kgen_array_sumcheck(printname // "%x_hat_disk", kgen_array_sum, DBLE(SUM(var%x_hat_disk, mask=(var%x_hat_disk .eq. &
          &var%x_hat_disk))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // "%x_hat_disk)) = ", DBLE(SUM(var%x_hat_disk, &
              &mask=(var%x_hat_disk .eq. var%x_hat_disk))) 
          END IF   
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%userprop, kgen_unit, printname // "%userprop", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%userprop, kgen_unit, printname // "%userprop", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%cantangle, kgen_unit, printname // "%cantangle", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%cantangle, kgen_unit, printname // "%cantangle", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%drdz, kgen_unit, printname // "%drdz", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%drdz, kgen_unit, printname // "%drdz", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_inputtype_real__reki_dim2(var%toeangle, kgen_unit, printname // "%toeangle", .TRUE.) 
      ELSE 
          CALL kr_bemt_inputtype_real__reki_dim2(var%toeangle, kgen_unit, printname // "%toeangle", .FALSE.) 
      END IF   
        
  END SUBROUTINE kr_bemt_types_bemt_inputtype 
    
  !write state subroutine for kr_bemt_inputtype_real__reki_dim2 
  SUBROUTINE kr_bemt_inputtype_real__reki_dim2(var, kgen_unit, printname, printvar) 
      REAL(KIND=reki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:,:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1, idx2 
      INTEGER, DIMENSION(2,2) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          READ (UNIT = kgen_unit) kgen_bound(1, 2) 
          READ (UNIT = kgen_unit) kgen_bound(2, 2) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1), kgen_bound(1,2):kgen_bound(2,2))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_bemt_inputtype_real__reki_dim2 
    
  !write state subroutine for kr_bemt_inputtype_real__reki_dim1 
  SUBROUTINE kr_bemt_inputtype_real__reki_dim1(var, kgen_unit, printname, printvar) 
      REAL(KIND=reki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1 
      INTEGER, DIMENSION(2,1) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_bemt_inputtype_real__reki_dim1 
    
  !read state subroutine for kr_bemt_types_bemt_outputtype 
  RECURSIVE SUBROUTINE kr_bemt_types_bemt_outputtype(var, kgen_unit, printname, printvar) 
      TYPE(bemt_outputtype), INTENT(INOUT) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%vrel, kgen_unit, printname // "%vrel", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%vrel, kgen_unit, printname // "%vrel", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%phi, kgen_unit, printname // "%phi", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%phi, kgen_unit, printname // "%phi", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%axinduction, kgen_unit, printname // "%axinduction", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%axinduction, kgen_unit, printname // "%axinduction", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%taninduction, kgen_unit, printname // "%taninduction", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%taninduction, kgen_unit, printname // "%taninduction", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%axinduction_qs, kgen_unit, printname // "%axinduction_qs", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%axinduction_qs, kgen_unit, printname // "%axinduction_qs", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%taninduction_qs, kgen_unit, printname // "%taninduction_qs", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%taninduction_qs, kgen_unit, printname // "%taninduction_qs", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%k, kgen_unit, printname // "%k", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%k, kgen_unit, printname // "%k", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%k_p, kgen_unit, printname // "%k_p", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%k_p, kgen_unit, printname // "%k_p", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%f, kgen_unit, printname // "%f", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%f, kgen_unit, printname // "%f", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%re, kgen_unit, printname // "%re", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%re, kgen_unit, printname // "%re", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%aoa, kgen_unit, printname // "%aoa", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%aoa, kgen_unit, printname // "%aoa", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cx, kgen_unit, printname // "%cx", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cx, kgen_unit, printname // "%cx", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cy, kgen_unit, printname // "%cy", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cy, kgen_unit, printname // "%cy", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cz, kgen_unit, printname // "%cz", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cz, kgen_unit, printname // "%cz", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cmx, kgen_unit, printname // "%cmx", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cmx, kgen_unit, printname // "%cmx", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cmy, kgen_unit, printname // "%cmy", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cmy, kgen_unit, printname // "%cmy", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cmz, kgen_unit, printname // "%cmz", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cmz, kgen_unit, printname // "%cmz", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cm, kgen_unit, printname // "%cm", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cm, kgen_unit, printname // "%cm", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cl, kgen_unit, printname // "%cl", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cl, kgen_unit, printname // "%cl", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cd, kgen_unit, printname // "%cd", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cd, kgen_unit, printname // "%cd", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%chi, kgen_unit, printname // "%chi", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%chi, kgen_unit, printname // "%chi", .FALSE.) 
      END IF   
        
      IF (PRESENT( printvar ) .AND. printvar) THEN 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cpmin, kgen_unit, printname // "%cpmin", .TRUE.) 
      ELSE 
          CALL kr_bemt_outputtype_real__reki_dim2(var%cpmin, kgen_unit, printname // "%cpmin", .FALSE.) 
      END IF   
        
  END SUBROUTINE kr_bemt_types_bemt_outputtype 
    
  !write state subroutine for kr_bemt_outputtype_real__reki_dim2 
  SUBROUTINE kr_bemt_outputtype_real__reki_dim2(var, kgen_unit, printname, printvar) 
      REAL(KIND=reki), INTENT(INOUT), ALLOCATABLE, DIMENSION(:,:) :: var 
      INTEGER, INTENT(IN) :: kgen_unit 
      CHARACTER(LEN=*), INTENT(IN) :: printname 
      LOGICAL, INTENT(IN), OPTIONAL :: printvar 
      LOGICAL :: kgen_istrue 
      REAL(KIND=8) :: kgen_array_sum 
      INTEGER :: idx1, idx2 
      INTEGER, DIMENSION(2,2) :: kgen_bound 
        
      READ (UNIT = kgen_unit) kgen_istrue 
      IF (kgen_istrue) THEN 
          IF (ALLOCATED( var )) THEN 
              DEALLOCATE (var) 
          END IF   
          READ (UNIT = kgen_unit) kgen_array_sum 
          READ (UNIT = kgen_unit) kgen_bound(1, 1) 
          READ (UNIT = kgen_unit) kgen_bound(2, 1) 
          READ (UNIT = kgen_unit) kgen_bound(1, 2) 
          READ (UNIT = kgen_unit) kgen_bound(2, 2) 
          ALLOCATE (var(kgen_bound(1,1):kgen_bound(2,1), kgen_bound(1,2):kgen_bound(2,2))) 
          READ (UNIT = kgen_unit) var 
          CALL kgen_array_sumcheck(printname, kgen_array_sum, DBLE(SUM(var, mask=(var .eq. var))), .TRUE.) 
          IF (PRESENT( printvar ) .AND. printvar) THEN 
              WRITE (*, *) "KGEN DEBUG: DBLE(SUM(" // printname // ")) = ", DBLE(SUM(var, mask=(var .eq. var))) 
          END IF   
      END IF   
        
  END SUBROUTINE kr_bemt_outputtype_real__reki_dim2 
    
  !verify state subroutine for kv_bemt_types_bemt_parametertype 
  RECURSIVE SUBROUTINE kv_bemt_types_bemt_parametertype(varname, check_status, var, kgenref_var) 
      CHARACTER(LEN=*), INTENT(IN) :: varname 
      TYPE(check_t), INTENT(INOUT) :: check_status 
      TYPE(bemt_parametertype), INTENT(IN) :: var, kgenref_var 
      TYPE(check_t) :: dtype_check_status, comp_check_status 
      INTEGER :: check_result 
      LOGICAL :: is_print = .FALSE. 
        
      real(KIND=dbki) :: diff_dt 
      INTEGER :: n_chord 
      real(KIND=reki) :: nrmsdiff_chord, rmsdiff_chord 
      real(KIND=reki), ALLOCATABLE :: buf1_chord(:,:), buf2_chord(:,:) 
      integer(KIND=intki) :: diff_numblades 
      real(KIND=reki) :: diff_airdens 
      real(KIND=reki) :: diff_kinvisc 
      integer(KIND=intki) :: diff_skewwakemod 
      integer(KIND=intki) :: diff_skewredistrmod 
      real(KIND=reki) :: diff_atol 
      integer(KIND=intki) :: diff_numbladenodes 
      integer(KIND=intki) :: diff_numreiterations 
      integer(KIND=intki) :: diff_maxinditerations 
      INTEGER :: n_afindx 
      integer(KIND=intki) :: nrmsdiff_afindx, rmsdiff_afindx 
      integer(KIND=intki), ALLOCATABLE :: buf1_afindx(:,:), buf2_afindx(:,:) 
      INTEGER :: n_tiplossconst 
      real(KIND=reki) :: nrmsdiff_tiplossconst, rmsdiff_tiplossconst 
      real(KIND=reki), ALLOCATABLE :: buf1_tiplossconst(:,:), buf2_tiplossconst(:,:) 
      INTEGER :: n_hublossconst 
      real(KIND=reki) :: nrmsdiff_hublossconst, rmsdiff_hublossconst 
      real(KIND=reki), ALLOCATABLE :: buf1_hublossconst(:,:), buf2_hublossconst(:,:) 
      INTEGER :: n_zhub 
      real(KIND=reki) :: nrmsdiff_zhub, rmsdiff_zhub 
      real(KIND=reki), ALLOCATABLE :: buf1_zhub(:), buf2_zhub(:) 
      integer(KIND=intki) :: diff_dbemt_mod 
      real(KIND=reki) :: diff_yawcorrfactor 
      INTEGER :: n_fixedinductions 
      real(KIND=reki) :: diff_rtipfixmax 
      INTEGER :: n_integrateweight 
      real(KIND=reki) :: nrmsdiff_integrateweight, rmsdiff_integrateweight 
      real(KIND=reki), ALLOCATABLE :: buf1_integrateweight(:,:), buf2_integrateweight(:,:) 
      integer(KIND=intki) :: diff_lin_nx 
      integer(KIND=intki) :: diff_bem_mod 
        
      check_status%numTotal = check_status%numTotal + 1 
        
      CALL kgen_init_check(dtype_check_status, rank=check_status%rank) 
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%dt == kgenref_var%dt) .OR. ((var%dt /= var%dt) .AND. (kgenref_var%dt /= kgenref_var%dt))) THEN
        IF (var%dt /= var%dt) WRITE(*, *) "localvar%dt is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%dt is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_dt = ABS(var%dt - kgenref_var%dt) 
          IF (diff_dt <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%dt is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%dt is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_dt 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_dt 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (ALLOCATED(var%chord)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%chord == kgenref_var%chord)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%chord is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_chord(SIZE(var%chord,dim=1),SIZE(var%chord,dim=2))) 
              ALLOCATE (buf2_chord(SIZE(var%chord,dim=1),SIZE(var%chord,dim=2))) 
              n_chord = COUNT(var%chord /= kgenref_var%chord) 
              WHERE ( ABS(kgenref_var%chord) > kgen_minvalue ) 
                  buf1_chord = ((var%chord-kgenref_var%chord)/kgenref_var%chord)**2 
                  buf2_chord = (var%chord-kgenref_var%chord)**2 
              ELSEWHERE 
                  buf1_chord = (var%chord-kgenref_var%chord)**2 
                  buf2_chord = buf1_chord 
              END WHERE   
              nrmsdiff_chord = SQRT(SUM(buf1_chord)/REAL(n_chord)) 
              rmsdiff_chord = SQRT(SUM(buf2_chord)/REAL(n_chord)) 
              IF (rmsdiff_chord > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%chord is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%chord is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%chord /= kgenref_var%chord), " of ", size( var%chord ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%chord)/real(size(var%chord)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%chord)/real(size(kgenref_var%chord)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_chord 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_chord 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%chord /= kgenref_var%chord), " of ", size( var%chord ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%chord)/real(size(var%chord)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%chord)/real(size(kgenref_var%chord)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_chord 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_chord 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%numblades == kgenref_var%numblades) .OR. ((var%numblades /= var%numblades) .AND. (kgenref_var%numblades /= kgenref_var%numblades))) THEN
        IF (var%numblades /= var%numblades) WRITE(*, *) "localvar%numblades is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%numblades is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_numblades = ABS(var%numblades - kgenref_var%numblades) 
          IF (diff_numblades <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%numblades is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%numblades is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_numblades 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_numblades 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%airdens == kgenref_var%airdens) .OR. ((var%airdens /= var%airdens) .AND. (kgenref_var%airdens /= kgenref_var%airdens))) THEN
        IF (var%airdens /= var%airdens) WRITE(*, *) "localvar%airdens is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%airdens is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_airdens = ABS(var%airdens - kgenref_var%airdens) 
          IF (diff_airdens <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%airdens is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%airdens is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_airdens 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_airdens 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%kinvisc == kgenref_var%kinvisc) .OR. ((var%kinvisc /= var%kinvisc) .AND. (kgenref_var%kinvisc /= kgenref_var%kinvisc))) THEN
        IF (var%kinvisc /= var%kinvisc) WRITE(*, *) "localvar%kinvisc is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%kinvisc is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_kinvisc = ABS(var%kinvisc - kgenref_var%kinvisc) 
          IF (diff_kinvisc <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%kinvisc is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%kinvisc is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_kinvisc 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_kinvisc 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%skewwakemod == kgenref_var%skewwakemod) .OR. ((var%skewwakemod /= var%skewwakemod) .AND. (kgenref_var%skewwakemod /= kgenref_var%skewwakemod))) THEN
        IF (var%skewwakemod /= var%skewwakemod) WRITE(*, *) "localvar%skewwakemod is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%skewwakemod is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_skewwakemod = ABS(var%skewwakemod - kgenref_var%skewwakemod) 
          IF (diff_skewwakemod <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%skewwakemod is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%skewwakemod is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_skewwakemod 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_skewwakemod 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%skewredistrmod == kgenref_var%skewredistrmod) .OR. ((var%skewredistrmod /= var%skewredistrmod) .AND. (kgenref_var%skewredistrmod /= kgenref_var%skewredistrmod))) THEN
        IF (var%skewredistrmod /= var%skewredistrmod) WRITE(*, *) "localvar%skewredistrmod is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%skewredistrmod is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_skewredistrmod = ABS(var%skewredistrmod - kgenref_var%skewredistrmod) 
          IF (diff_skewredistrmod <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%skewredistrmod is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%skewredistrmod is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_skewredistrmod 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_skewredistrmod 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%atol == kgenref_var%atol) .OR. ((var%atol /= var%atol) .AND. (kgenref_var%atol /= kgenref_var%atol))) THEN
        IF (var%atol /= var%atol) WRITE(*, *) "localvar%atol is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%atol is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_atol = ABS(var%atol - kgenref_var%atol) 
          IF (diff_atol <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%atol is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%atol is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_atol 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_atol 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%usetiploss .EQV. kgenref_var%usetiploss) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%usetiploss is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%usetiploss is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%usehubloss .EQV. kgenref_var%usehubloss) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%usehubloss is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%usehubloss is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%useinduction .EQV. kgenref_var%useinduction) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%useinduction is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%useinduction is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%usetanind .EQV. kgenref_var%usetanind) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%usetanind is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%usetanind is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%useaidrag .EQV. kgenref_var%useaidrag) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%useaidrag is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%useaidrag is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%usetidrag .EQV. kgenref_var%usetidrag) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%usetidrag is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%usetidrag is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%numbladenodes == kgenref_var%numbladenodes) .OR. ((var%numbladenodes /= var%numbladenodes) .AND. (kgenref_var%numbladenodes /= kgenref_var%numbladenodes))) THEN
        IF (var%numbladenodes /= var%numbladenodes) WRITE(*, *) "localvar%numbladenodes is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%numbladenodes is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_numbladenodes = ABS(var%numbladenodes - kgenref_var%numbladenodes) 
          IF (diff_numbladenodes <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%numbladenodes is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%numbladenodes is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_numbladenodes 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_numbladenodes 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%numreiterations == kgenref_var%numreiterations) .OR. ((var%numreiterations /= var%numreiterations) .AND. (kgenref_var%numreiterations /= kgenref_var%numreiterations))) THEN
        IF (var%numreiterations /= var%numreiterations) WRITE(*, *) "localvar%numreiterations is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%numreiterations is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_numreiterations = ABS(var%numreiterations - kgenref_var%numreiterations) 
          IF (diff_numreiterations <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%numreiterations is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%numreiterations is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_numreiterations 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_numreiterations 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%maxinditerations == kgenref_var%maxinditerations) .OR. ((var%maxinditerations /= var%maxinditerations) .AND. (kgenref_var%maxinditerations /= kgenref_var%maxinditerations))) THEN
        IF (var%maxinditerations /= var%maxinditerations) WRITE(*, *) "localvar%maxinditerations is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%maxinditerations is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_maxinditerations = ABS(var%maxinditerations - kgenref_var%maxinditerations) 
          IF (diff_maxinditerations <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%maxinditerations is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%maxinditerations is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_maxinditerations 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_maxinditerations 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (ALLOCATED(var%afindx)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%afindx == kgenref_var%afindx)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%afindx is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_afindx(SIZE(var%afindx,dim=1),SIZE(var%afindx,dim=2))) 
              ALLOCATE (buf2_afindx(SIZE(var%afindx,dim=1),SIZE(var%afindx,dim=2))) 
              n_afindx = COUNT(var%afindx /= kgenref_var%afindx) 
              WHERE ( ABS(kgenref_var%afindx) > kgen_minvalue ) 
                  buf1_afindx = ((var%afindx-kgenref_var%afindx)/kgenref_var%afindx)**2 
                  buf2_afindx = (var%afindx-kgenref_var%afindx)**2 
              ELSEWHERE 
                  buf1_afindx = (var%afindx-kgenref_var%afindx)**2 
                  buf2_afindx = buf1_afindx 
              END WHERE   
              nrmsdiff_afindx = SQRT(SUM(buf1_afindx)/REAL(n_afindx)) 
              rmsdiff_afindx = SQRT(SUM(buf2_afindx)/REAL(n_afindx)) 
              IF (rmsdiff_afindx > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%afindx is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%afindx is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%afindx /= kgenref_var%afindx), " of ", size( var%afindx ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%afindx)/real(size(var%afindx)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%afindx)/real(size(kgenref_var%afindx)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_afindx 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_afindx 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%afindx /= kgenref_var%afindx), " of ", size( var%afindx ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%afindx)/real(size(var%afindx)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%afindx)/real(size(kgenref_var%afindx)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_afindx 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_afindx 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%tiplossconst)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%tiplossconst == kgenref_var%tiplossconst)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%tiplossconst is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_tiplossconst(SIZE(var%tiplossconst,dim=1),SIZE(var%tiplossconst,dim=2))) 
              ALLOCATE (buf2_tiplossconst(SIZE(var%tiplossconst,dim=1),SIZE(var%tiplossconst,dim=2))) 
              n_tiplossconst = COUNT(var%tiplossconst /= kgenref_var%tiplossconst) 
              WHERE ( ABS(kgenref_var%tiplossconst) > kgen_minvalue ) 
                  buf1_tiplossconst = ((var%tiplossconst-kgenref_var%tiplossconst)/kgenref_var%tiplossconst)**2 
                  buf2_tiplossconst = (var%tiplossconst-kgenref_var%tiplossconst)**2 
              ELSEWHERE 
                  buf1_tiplossconst = (var%tiplossconst-kgenref_var%tiplossconst)**2 
                  buf2_tiplossconst = buf1_tiplossconst 
              END WHERE   
              nrmsdiff_tiplossconst = SQRT(SUM(buf1_tiplossconst)/REAL(n_tiplossconst)) 
              rmsdiff_tiplossconst = SQRT(SUM(buf2_tiplossconst)/REAL(n_tiplossconst)) 
              IF (rmsdiff_tiplossconst > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%tiplossconst is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%tiplossconst is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%tiplossconst /= kgenref_var%tiplossconst), " of ", size( var%tiplossconst ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%tiplossconst)/real(size(var%tiplossconst)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%tiplossconst)/real(size(kgenref_var%tiplossconst)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_tiplossconst 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_tiplossconst 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%tiplossconst /= kgenref_var%tiplossconst), " of ", size( var%tiplossconst ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%tiplossconst)/real(size(var%tiplossconst)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%tiplossconst)/real(size(kgenref_var%tiplossconst)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_tiplossconst 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_tiplossconst 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%hublossconst)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%hublossconst == kgenref_var%hublossconst)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%hublossconst is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_hublossconst(SIZE(var%hublossconst,dim=1),SIZE(var%hublossconst,dim=2))) 
              ALLOCATE (buf2_hublossconst(SIZE(var%hublossconst,dim=1),SIZE(var%hublossconst,dim=2))) 
              n_hublossconst = COUNT(var%hublossconst /= kgenref_var%hublossconst) 
              WHERE ( ABS(kgenref_var%hublossconst) > kgen_minvalue ) 
                  buf1_hublossconst = ((var%hublossconst-kgenref_var%hublossconst)/kgenref_var%hublossconst)**2 
                  buf2_hublossconst = (var%hublossconst-kgenref_var%hublossconst)**2 
              ELSEWHERE 
                  buf1_hublossconst = (var%hublossconst-kgenref_var%hublossconst)**2 
                  buf2_hublossconst = buf1_hublossconst 
              END WHERE   
              nrmsdiff_hublossconst = SQRT(SUM(buf1_hublossconst)/REAL(n_hublossconst)) 
              rmsdiff_hublossconst = SQRT(SUM(buf2_hublossconst)/REAL(n_hublossconst)) 
              IF (rmsdiff_hublossconst > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%hublossconst is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%hublossconst is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%hublossconst /= kgenref_var%hublossconst), " of ", size( var%hublossconst ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%hublossconst)/real(size(var%hublossconst)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%hublossconst)/real(size(kgenref_var%hublossconst)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_hublossconst 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_hublossconst 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%hublossconst /= kgenref_var%hublossconst), " of ", size( var%hublossconst ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%hublossconst)/real(size(var%hublossconst)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%hublossconst)/real(size(kgenref_var%hublossconst)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_hublossconst 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_hublossconst 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%zhub)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%zhub == kgenref_var%zhub)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%zhub is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_zhub(SIZE(var%zhub,dim=1))) 
              ALLOCATE (buf2_zhub(SIZE(var%zhub,dim=1))) 
              n_zhub = COUNT(var%zhub /= kgenref_var%zhub) 
              WHERE ( ABS(kgenref_var%zhub) > kgen_minvalue ) 
                  buf1_zhub = ((var%zhub-kgenref_var%zhub)/kgenref_var%zhub)**2 
                  buf2_zhub = (var%zhub-kgenref_var%zhub)**2 
              ELSEWHERE 
                  buf1_zhub = (var%zhub-kgenref_var%zhub)**2 
                  buf2_zhub = buf1_zhub 
              END WHERE   
              nrmsdiff_zhub = SQRT(SUM(buf1_zhub)/REAL(n_zhub)) 
              rmsdiff_zhub = SQRT(SUM(buf2_zhub)/REAL(n_zhub)) 
              IF (rmsdiff_zhub > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%zhub is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%zhub is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%zhub /= kgenref_var%zhub), " of ", size( var%zhub ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%zhub)/real(size(var%zhub)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%zhub)/real(size(kgenref_var%zhub)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_zhub 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_zhub 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%zhub /= kgenref_var%zhub), " of ", size( var%zhub ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%zhub)/real(size(var%zhub)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%zhub)/real(size(kgenref_var%zhub)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_zhub 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_zhub 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      CALL kgen_init_check(comp_check_status, rank=check_status%rank) 
      CALL kv_unsteadyaero_types_ua_parametertype("ua", comp_check_status, var%ua, kgenref_var%ua) 
      IF (comp_check_status%numTotal == comp_check_status%numIdentical) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname))//"%ua", " is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE IF (comp_check_status%numOutTol > 0) THEN 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%ua is NOT IDENTICAL(out of tolerance)." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      ELSE IF (comp_check_status%numInTol > 0) THEN 
          dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%ua is NOT IDENTICAL(within tolerance)." 
              END IF   
          END IF   
          check_result = CHECK_IN_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "       number of components             : ", comp_check_status%numtotal 
                  WRITE (*, *) "       identical                                   : ", comp_check_status%numidentical 
                  WRITE (*, *) "       not identical - out of tol.: ", comp_check_status%numouttol 
                  WRITE (*, *) "       not identical - within tol.: ", comp_check_status%numintol 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "       number of components             : ", comp_check_status%numtotal 
                  WRITE (*, *) "       identical                                   : ", comp_check_status%numidentical 
                  WRITE (*, *) "       not identical - out of tol.: ", comp_check_status%numouttol 
                  WRITE (*, *) "       not identical - within tol.: ", comp_check_status%numintol 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      CALL kgen_init_check(comp_check_status, rank=check_status%rank) 
      CALL kv_dbemt_types_dbemt_parametertype("dbemt", comp_check_status, var%dbemt, kgenref_var%dbemt) 
      IF (comp_check_status%numTotal == comp_check_status%numIdentical) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname))//"%dbemt", " is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE IF (comp_check_status%numOutTol > 0) THEN 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%dbemt is NOT IDENTICAL(out of tolerance)." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      ELSE IF (comp_check_status%numInTol > 0) THEN 
          dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%dbemt is NOT IDENTICAL(within tolerance)." 
              END IF   
          END IF   
          check_result = CHECK_IN_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "       number of components             : ", comp_check_status%numtotal 
                  WRITE (*, *) "       identical                                   : ", comp_check_status%numidentical 
                  WRITE (*, *) "       not identical - out of tol.: ", comp_check_status%numouttol 
                  WRITE (*, *) "       not identical - within tol.: ", comp_check_status%numintol 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "       number of components             : ", comp_check_status%numtotal 
                  WRITE (*, *) "       identical                                   : ", comp_check_status%numidentical 
                  WRITE (*, *) "       not identical - out of tol.: ", comp_check_status%numouttol 
                  WRITE (*, *) "       not identical - within tol.: ", comp_check_status%numintol 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%ua_flag .EQV. kgenref_var%ua_flag) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%ua_flag is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%ua_flag is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%dbemt_mod == kgenref_var%dbemt_mod) .OR. ((var%dbemt_mod /= var%dbemt_mod) .AND. (kgenref_var%dbemt_mod /= kgenref_var%dbemt_mod))) THEN
        IF (var%dbemt_mod /= var%dbemt_mod) WRITE(*, *) "localvar%dbemt_mod is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%dbemt_mod is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_dbemt_mod = ABS(var%dbemt_mod - kgenref_var%dbemt_mod) 
          IF (diff_dbemt_mod <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%dbemt_mod is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%dbemt_mod is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_dbemt_mod 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_dbemt_mod 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%yawcorrfactor == kgenref_var%yawcorrfactor) .OR. ((var%yawcorrfactor /= var%yawcorrfactor) .AND. (kgenref_var%yawcorrfactor /= kgenref_var%yawcorrfactor))) THEN
        IF (var%yawcorrfactor /= var%yawcorrfactor) WRITE(*, *) "localvar%yawcorrfactor is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%yawcorrfactor is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_yawcorrfactor = ABS(var%yawcorrfactor - kgenref_var%yawcorrfactor) 
          IF (diff_yawcorrfactor <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%yawcorrfactor is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%yawcorrfactor is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_yawcorrfactor 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_yawcorrfactor 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (ALLOCATED(var%fixedinductions)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%fixedinductions .EQV. kgenref_var%fixedinductions)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%fixedinductions is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              n_fixedinductions = COUNT(var%fixedinductions .NEQV. kgenref_var%fixedinductions) 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%fixedinductions is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) "NOT IMPLEMENTED YET" 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) "NOT IMPLEMENTED YET" 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (var%momentumcorr .EQV. kgenref_var%momentumcorr) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%momentumcorr is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
          IF (kgen_verboseLevel > 1) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%momentumcorr is NOT IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_OUT_TOL 
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "NOT IMPLEMENTED YET" 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%rtipfixmax == kgenref_var%rtipfixmax) .OR. ((var%rtipfixmax /= var%rtipfixmax) .AND. (kgenref_var%rtipfixmax /= kgenref_var%rtipfixmax))) THEN
        IF (var%rtipfixmax /= var%rtipfixmax) WRITE(*, *) "localvar%rtipfixmax is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%rtipfixmax is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_rtipfixmax = ABS(var%rtipfixmax - kgenref_var%rtipfixmax) 
          IF (diff_rtipfixmax <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%rtipfixmax is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%rtipfixmax is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_rtipfixmax 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_rtipfixmax 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (ALLOCATED(var%integrateweight)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%integrateweight == kgenref_var%integrateweight)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%integrateweight is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_integrateweight(SIZE(var%integrateweight,dim=1),SIZE(var%integrateweight,dim=2))) 
              ALLOCATE (buf2_integrateweight(SIZE(var%integrateweight,dim=1),SIZE(var%integrateweight,dim=2))) 
              n_integrateweight = COUNT(var%integrateweight /= kgenref_var%integrateweight) 
              WHERE ( ABS(kgenref_var%integrateweight) > kgen_minvalue ) 
                  buf1_integrateweight = ((var%integrateweight-kgenref_var%integrateweight)/kgenref_var%integrateweight)**2 
                  buf2_integrateweight = (var%integrateweight-kgenref_var%integrateweight)**2 
              ELSEWHERE 
                  buf1_integrateweight = (var%integrateweight-kgenref_var%integrateweight)**2 
                  buf2_integrateweight = buf1_integrateweight 
              END WHERE   
              nrmsdiff_integrateweight = SQRT(SUM(buf1_integrateweight)/REAL(n_integrateweight)) 
              rmsdiff_integrateweight = SQRT(SUM(buf2_integrateweight)/REAL(n_integrateweight)) 
              IF (rmsdiff_integrateweight > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%integrateweight is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%integrateweight is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%integrateweight /= kgenref_var%integrateweight), " of ", size( var%integrateweight &
                      &), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%integrateweight)/real(size(var%integrateweight)) 
                      WRITE (*, *) "Average - reference ", &
                      &sum(kgenref_var%integrateweight)/real(size(kgenref_var%integrateweight)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_integrateweight 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_integrateweight 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%integrateweight /= kgenref_var%integrateweight), " of ", size( var%integrateweight &
                      &), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%integrateweight)/real(size(var%integrateweight)) 
                      WRITE (*, *) "Average - reference ", &
                      &sum(kgenref_var%integrateweight)/real(size(kgenref_var%integrateweight)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_integrateweight 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_integrateweight 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%lin_nx == kgenref_var%lin_nx) .OR. ((var%lin_nx /= var%lin_nx) .AND. (kgenref_var%lin_nx /= kgenref_var%lin_nx))) THEN
        IF (var%lin_nx /= var%lin_nx) WRITE(*, *) "localvar%lin_nx is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%lin_nx is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_lin_nx = ABS(var%lin_nx - kgenref_var%lin_nx) 
          IF (diff_lin_nx <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%lin_nx is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%lin_nx is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_lin_nx 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_lin_nx 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%bem_mod == kgenref_var%bem_mod) .OR. ((var%bem_mod /= var%bem_mod) .AND. (kgenref_var%bem_mod /= kgenref_var%bem_mod))) THEN
        IF (var%bem_mod /= var%bem_mod) WRITE(*, *) "localvar%bem_mod is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%bem_mod is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_bem_mod = ABS(var%bem_mod - kgenref_var%bem_mod) 
          IF (diff_bem_mod <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%bem_mod is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%bem_mod is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_bem_mod 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_bem_mod 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (dtype_check_status%numTotal == dtype_check_status%numIdentical) THEN 
          check_status%numIdentical = check_status%numIdentical + 1 
      ELSE IF (dtype_check_status%numOutTol > 0) THEN 
          check_status%numOutTol = check_status%numOutTol + 1 
      ELSE IF (dtype_check_status%numInTol > 0) THEN 
          check_status%numInTol = check_status%numInTol + 1 
      END IF   
  END SUBROUTINE kv_bemt_types_bemt_parametertype 
    
  !verify state subroutine for kv_bemt_types_bemt_inputtype 
  RECURSIVE SUBROUTINE kv_bemt_types_bemt_inputtype(varname, check_status, var, kgenref_var) 
      CHARACTER(LEN=*), INTENT(IN) :: varname 
      TYPE(check_t), INTENT(INOUT) :: check_status 
      TYPE(bemt_inputtype), INTENT(IN) :: var, kgenref_var 
      TYPE(check_t) :: dtype_check_status, comp_check_status 
      INTEGER :: check_result 
      LOGICAL :: is_print = .FALSE. 
        
      INTEGER :: n_theta 
      real(KIND=reki) :: nrmsdiff_theta, rmsdiff_theta 
      real(KIND=reki), ALLOCATABLE :: buf1_theta(:,:), buf2_theta(:,:) 
      real(KIND=reki) :: diff_chi0 
      real(KIND=reki) :: diff_psiskewoffset 
      INTEGER :: n_psi_s 
      real(KIND=reki) :: nrmsdiff_psi_s, rmsdiff_psi_s 
      real(KIND=reki), ALLOCATABLE :: buf1_psi_s(:), buf2_psi_s(:) 
      real(KIND=reki) :: diff_omega 
      real(KIND=reki) :: diff_tsr 
      INTEGER :: n_vx 
      real(KIND=reki) :: nrmsdiff_vx, rmsdiff_vx 
      real(KIND=reki), ALLOCATABLE :: buf1_vx(:,:), buf2_vx(:,:) 
      INTEGER :: n_vy 
      real(KIND=reki) :: nrmsdiff_vy, rmsdiff_vy 
      real(KIND=reki), ALLOCATABLE :: buf1_vy(:,:), buf2_vy(:,:) 
      INTEGER :: n_vz 
      real(KIND=reki) :: nrmsdiff_vz, rmsdiff_vz 
      real(KIND=reki), ALLOCATABLE :: buf1_vz(:,:), buf2_vz(:,:) 
      INTEGER :: n_omega_z 
      real(KIND=reki) :: nrmsdiff_omega_z, rmsdiff_omega_z 
      real(KIND=reki), ALLOCATABLE :: buf1_omega_z(:,:), buf2_omega_z(:,:) 
      INTEGER :: n_xvelcorr 
      real(KIND=reki) :: nrmsdiff_xvelcorr, rmsdiff_xvelcorr 
      real(KIND=reki), ALLOCATABLE :: buf1_xvelcorr(:,:), buf2_xvelcorr(:,:) 
      INTEGER :: n_rlocal 
      real(KIND=reki) :: nrmsdiff_rlocal, rmsdiff_rlocal 
      real(KIND=reki), ALLOCATABLE :: buf1_rlocal(:,:), buf2_rlocal(:,:) 
      real(KIND=reki) :: diff_un_disk 
      INTEGER :: n_v0 
      real(KIND=reki) :: nrmsdiff_v0, rmsdiff_v0 
      real(KIND=reki), ALLOCATABLE :: buf1_v0(:), buf2_v0(:) 
      INTEGER :: n_x_hat_disk 
      real(KIND=r8ki) :: nrmsdiff_x_hat_disk, rmsdiff_x_hat_disk 
      real(KIND=r8ki), ALLOCATABLE :: buf1_x_hat_disk(:), buf2_x_hat_disk(:) 
      INTEGER :: n_userprop 
      real(KIND=reki) :: nrmsdiff_userprop, rmsdiff_userprop 
      real(KIND=reki), ALLOCATABLE :: buf1_userprop(:,:), buf2_userprop(:,:) 
      INTEGER :: n_cantangle 
      real(KIND=reki) :: nrmsdiff_cantangle, rmsdiff_cantangle 
      real(KIND=reki), ALLOCATABLE :: buf1_cantangle(:,:), buf2_cantangle(:,:) 
      INTEGER :: n_drdz 
      real(KIND=reki) :: nrmsdiff_drdz, rmsdiff_drdz 
      real(KIND=reki), ALLOCATABLE :: buf1_drdz(:,:), buf2_drdz(:,:) 
      INTEGER :: n_toeangle 
      real(KIND=reki) :: nrmsdiff_toeangle, rmsdiff_toeangle 
      real(KIND=reki), ALLOCATABLE :: buf1_toeangle(:,:), buf2_toeangle(:,:) 
        
      check_status%numTotal = check_status%numTotal + 1 
        
      CALL kgen_init_check(dtype_check_status, rank=check_status%rank) 
      IF (ALLOCATED(var%theta)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%theta == kgenref_var%theta)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%theta is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_theta(SIZE(var%theta,dim=1),SIZE(var%theta,dim=2))) 
              ALLOCATE (buf2_theta(SIZE(var%theta,dim=1),SIZE(var%theta,dim=2))) 
              n_theta = COUNT(var%theta /= kgenref_var%theta) 
              WHERE ( ABS(kgenref_var%theta) > kgen_minvalue ) 
                  buf1_theta = ((var%theta-kgenref_var%theta)/kgenref_var%theta)**2 
                  buf2_theta = (var%theta-kgenref_var%theta)**2 
              ELSEWHERE 
                  buf1_theta = (var%theta-kgenref_var%theta)**2 
                  buf2_theta = buf1_theta 
              END WHERE   
              nrmsdiff_theta = SQRT(SUM(buf1_theta)/REAL(n_theta)) 
              rmsdiff_theta = SQRT(SUM(buf2_theta)/REAL(n_theta)) 
              IF (rmsdiff_theta > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%theta is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%theta is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%theta /= kgenref_var%theta), " of ", size( var%theta ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%theta)/real(size(var%theta)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%theta)/real(size(kgenref_var%theta)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_theta 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_theta 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%theta /= kgenref_var%theta), " of ", size( var%theta ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%theta)/real(size(var%theta)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%theta)/real(size(kgenref_var%theta)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_theta 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_theta 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%chi0 == kgenref_var%chi0) .OR. ((var%chi0 /= var%chi0) .AND. (kgenref_var%chi0 /= kgenref_var%chi0))) THEN
        IF (var%chi0 /= var%chi0) WRITE(*, *) "localvar%chi0 is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%chi0 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_chi0 = ABS(var%chi0 - kgenref_var%chi0) 
          IF (diff_chi0 <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%chi0 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%chi0 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_chi0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_chi0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%psiskewoffset == kgenref_var%psiskewoffset) .OR. ((var%psiskewoffset /= var%psiskewoffset) .AND. (kgenref_var%psiskewoffset /= kgenref_var%psiskewoffset))) THEN
        IF (var%psiskewoffset /= var%psiskewoffset) WRITE(*, *) "localvar%psiskewoffset is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%psiskewoffset is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_psiskewoffset = ABS(var%psiskewoffset - kgenref_var%psiskewoffset) 
          IF (diff_psiskewoffset <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%psiskewoffset is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%psiskewoffset is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_psiskewoffset 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_psiskewoffset 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (ALLOCATED(var%psi_s)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%psi_s == kgenref_var%psi_s)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%psi_s is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_psi_s(SIZE(var%psi_s,dim=1))) 
              ALLOCATE (buf2_psi_s(SIZE(var%psi_s,dim=1))) 
              n_psi_s = COUNT(var%psi_s /= kgenref_var%psi_s) 
              WHERE ( ABS(kgenref_var%psi_s) > kgen_minvalue ) 
                  buf1_psi_s = ((var%psi_s-kgenref_var%psi_s)/kgenref_var%psi_s)**2 
                  buf2_psi_s = (var%psi_s-kgenref_var%psi_s)**2 
              ELSEWHERE 
                  buf1_psi_s = (var%psi_s-kgenref_var%psi_s)**2 
                  buf2_psi_s = buf1_psi_s 
              END WHERE   
              nrmsdiff_psi_s = SQRT(SUM(buf1_psi_s)/REAL(n_psi_s)) 
              rmsdiff_psi_s = SQRT(SUM(buf2_psi_s)/REAL(n_psi_s)) 
              IF (rmsdiff_psi_s > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%psi_s is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%psi_s is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%psi_s /= kgenref_var%psi_s), " of ", size( var%psi_s ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%psi_s)/real(size(var%psi_s)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%psi_s)/real(size(kgenref_var%psi_s)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_psi_s 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_psi_s 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%psi_s /= kgenref_var%psi_s), " of ", size( var%psi_s ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%psi_s)/real(size(var%psi_s)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%psi_s)/real(size(kgenref_var%psi_s)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_psi_s 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_psi_s 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%omega == kgenref_var%omega) .OR. ((var%omega /= var%omega) .AND. (kgenref_var%omega /= kgenref_var%omega))) THEN
        IF (var%omega /= var%omega) WRITE(*, *) "localvar%omega is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%omega is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_omega = ABS(var%omega - kgenref_var%omega) 
          IF (diff_omega <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%omega is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%omega is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_omega 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_omega 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%tsr == kgenref_var%tsr) .OR. ((var%tsr /= var%tsr) .AND. (kgenref_var%tsr /= kgenref_var%tsr))) THEN
        IF (var%tsr /= var%tsr) WRITE(*, *) "localvar%tsr is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%tsr is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_tsr = ABS(var%tsr - kgenref_var%tsr) 
          IF (diff_tsr <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%tsr is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%tsr is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_tsr 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_tsr 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (ALLOCATED(var%vx)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%vx == kgenref_var%vx)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%vx is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_vx(SIZE(var%vx,dim=1),SIZE(var%vx,dim=2))) 
              ALLOCATE (buf2_vx(SIZE(var%vx,dim=1),SIZE(var%vx,dim=2))) 
              n_vx = COUNT(var%vx /= kgenref_var%vx) 
              WHERE ( ABS(kgenref_var%vx) > kgen_minvalue ) 
                  buf1_vx = ((var%vx-kgenref_var%vx)/kgenref_var%vx)**2 
                  buf2_vx = (var%vx-kgenref_var%vx)**2 
              ELSEWHERE 
                  buf1_vx = (var%vx-kgenref_var%vx)**2 
                  buf2_vx = buf1_vx 
              END WHERE   
              nrmsdiff_vx = SQRT(SUM(buf1_vx)/REAL(n_vx)) 
              rmsdiff_vx = SQRT(SUM(buf2_vx)/REAL(n_vx)) 
              IF (rmsdiff_vx > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%vx is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%vx is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%vx /= kgenref_var%vx), " of ", size( var%vx ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%vx)/real(size(var%vx)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%vx)/real(size(kgenref_var%vx)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_vx 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_vx 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%vx /= kgenref_var%vx), " of ", size( var%vx ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%vx)/real(size(var%vx)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%vx)/real(size(kgenref_var%vx)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_vx 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_vx 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%vy)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%vy == kgenref_var%vy)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%vy is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_vy(SIZE(var%vy,dim=1),SIZE(var%vy,dim=2))) 
              ALLOCATE (buf2_vy(SIZE(var%vy,dim=1),SIZE(var%vy,dim=2))) 
              n_vy = COUNT(var%vy /= kgenref_var%vy) 
              WHERE ( ABS(kgenref_var%vy) > kgen_minvalue ) 
                  buf1_vy = ((var%vy-kgenref_var%vy)/kgenref_var%vy)**2 
                  buf2_vy = (var%vy-kgenref_var%vy)**2 
              ELSEWHERE 
                  buf1_vy = (var%vy-kgenref_var%vy)**2 
                  buf2_vy = buf1_vy 
              END WHERE   
              nrmsdiff_vy = SQRT(SUM(buf1_vy)/REAL(n_vy)) 
              rmsdiff_vy = SQRT(SUM(buf2_vy)/REAL(n_vy)) 
              IF (rmsdiff_vy > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%vy is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%vy is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%vy /= kgenref_var%vy), " of ", size( var%vy ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%vy)/real(size(var%vy)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%vy)/real(size(kgenref_var%vy)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_vy 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_vy 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%vy /= kgenref_var%vy), " of ", size( var%vy ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%vy)/real(size(var%vy)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%vy)/real(size(kgenref_var%vy)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_vy 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_vy 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%vz)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%vz == kgenref_var%vz)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%vz is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_vz(SIZE(var%vz,dim=1),SIZE(var%vz,dim=2))) 
              ALLOCATE (buf2_vz(SIZE(var%vz,dim=1),SIZE(var%vz,dim=2))) 
              n_vz = COUNT(var%vz /= kgenref_var%vz) 
              WHERE ( ABS(kgenref_var%vz) > kgen_minvalue ) 
                  buf1_vz = ((var%vz-kgenref_var%vz)/kgenref_var%vz)**2 
                  buf2_vz = (var%vz-kgenref_var%vz)**2 
              ELSEWHERE 
                  buf1_vz = (var%vz-kgenref_var%vz)**2 
                  buf2_vz = buf1_vz 
              END WHERE   
              nrmsdiff_vz = SQRT(SUM(buf1_vz)/REAL(n_vz)) 
              rmsdiff_vz = SQRT(SUM(buf2_vz)/REAL(n_vz)) 
              IF (rmsdiff_vz > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%vz is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%vz is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%vz /= kgenref_var%vz), " of ", size( var%vz ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%vz)/real(size(var%vz)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%vz)/real(size(kgenref_var%vz)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_vz 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_vz 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%vz /= kgenref_var%vz), " of ", size( var%vz ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%vz)/real(size(var%vz)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%vz)/real(size(kgenref_var%vz)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_vz 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_vz 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%omega_z)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%omega_z == kgenref_var%omega_z)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%omega_z is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_omega_z(SIZE(var%omega_z,dim=1),SIZE(var%omega_z,dim=2))) 
              ALLOCATE (buf2_omega_z(SIZE(var%omega_z,dim=1),SIZE(var%omega_z,dim=2))) 
              n_omega_z = COUNT(var%omega_z /= kgenref_var%omega_z) 
              WHERE ( ABS(kgenref_var%omega_z) > kgen_minvalue ) 
                  buf1_omega_z = ((var%omega_z-kgenref_var%omega_z)/kgenref_var%omega_z)**2 
                  buf2_omega_z = (var%omega_z-kgenref_var%omega_z)**2 
              ELSEWHERE 
                  buf1_omega_z = (var%omega_z-kgenref_var%omega_z)**2 
                  buf2_omega_z = buf1_omega_z 
              END WHERE   
              nrmsdiff_omega_z = SQRT(SUM(buf1_omega_z)/REAL(n_omega_z)) 
              rmsdiff_omega_z = SQRT(SUM(buf2_omega_z)/REAL(n_omega_z)) 
              IF (rmsdiff_omega_z > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%omega_z is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%omega_z is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%omega_z /= kgenref_var%omega_z), " of ", size( var%omega_z ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%omega_z)/real(size(var%omega_z)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%omega_z)/real(size(kgenref_var%omega_z)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_omega_z 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_omega_z 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%omega_z /= kgenref_var%omega_z), " of ", size( var%omega_z ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%omega_z)/real(size(var%omega_z)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%omega_z)/real(size(kgenref_var%omega_z)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_omega_z 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_omega_z 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%xvelcorr)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%xvelcorr == kgenref_var%xvelcorr)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%xvelcorr is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_xvelcorr(SIZE(var%xvelcorr,dim=1),SIZE(var%xvelcorr,dim=2))) 
              ALLOCATE (buf2_xvelcorr(SIZE(var%xvelcorr,dim=1),SIZE(var%xvelcorr,dim=2))) 
              n_xvelcorr = COUNT(var%xvelcorr /= kgenref_var%xvelcorr) 
              WHERE ( ABS(kgenref_var%xvelcorr) > kgen_minvalue ) 
                  buf1_xvelcorr = ((var%xvelcorr-kgenref_var%xvelcorr)/kgenref_var%xvelcorr)**2 
                  buf2_xvelcorr = (var%xvelcorr-kgenref_var%xvelcorr)**2 
              ELSEWHERE 
                  buf1_xvelcorr = (var%xvelcorr-kgenref_var%xvelcorr)**2 
                  buf2_xvelcorr = buf1_xvelcorr 
              END WHERE   
              nrmsdiff_xvelcorr = SQRT(SUM(buf1_xvelcorr)/REAL(n_xvelcorr)) 
              rmsdiff_xvelcorr = SQRT(SUM(buf2_xvelcorr)/REAL(n_xvelcorr)) 
              IF (rmsdiff_xvelcorr > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%xvelcorr is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%xvelcorr is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%xvelcorr /= kgenref_var%xvelcorr), " of ", size( var%xvelcorr ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%xvelcorr)/real(size(var%xvelcorr)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%xvelcorr)/real(size(kgenref_var%xvelcorr)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_xvelcorr 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_xvelcorr 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%xvelcorr /= kgenref_var%xvelcorr), " of ", size( var%xvelcorr ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%xvelcorr)/real(size(var%xvelcorr)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%xvelcorr)/real(size(kgenref_var%xvelcorr)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_xvelcorr 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_xvelcorr 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%rlocal)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%rlocal == kgenref_var%rlocal)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%rlocal is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_rlocal(SIZE(var%rlocal,dim=1),SIZE(var%rlocal,dim=2))) 
              ALLOCATE (buf2_rlocal(SIZE(var%rlocal,dim=1),SIZE(var%rlocal,dim=2))) 
              n_rlocal = COUNT(var%rlocal /= kgenref_var%rlocal) 
              WHERE ( ABS(kgenref_var%rlocal) > kgen_minvalue ) 
                  buf1_rlocal = ((var%rlocal-kgenref_var%rlocal)/kgenref_var%rlocal)**2 
                  buf2_rlocal = (var%rlocal-kgenref_var%rlocal)**2 
              ELSEWHERE 
                  buf1_rlocal = (var%rlocal-kgenref_var%rlocal)**2 
                  buf2_rlocal = buf1_rlocal 
              END WHERE   
              nrmsdiff_rlocal = SQRT(SUM(buf1_rlocal)/REAL(n_rlocal)) 
              rmsdiff_rlocal = SQRT(SUM(buf2_rlocal)/REAL(n_rlocal)) 
              IF (rmsdiff_rlocal > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%rlocal is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%rlocal is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%rlocal /= kgenref_var%rlocal), " of ", size( var%rlocal ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%rlocal)/real(size(var%rlocal)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%rlocal)/real(size(kgenref_var%rlocal)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_rlocal 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_rlocal 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%rlocal /= kgenref_var%rlocal), " of ", size( var%rlocal ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%rlocal)/real(size(var%rlocal)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%rlocal)/real(size(kgenref_var%rlocal)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_rlocal 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_rlocal 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF ((var%un_disk == kgenref_var%un_disk) .OR. ((var%un_disk /= var%un_disk) .AND. (kgenref_var%un_disk /= kgenref_var%un_disk))) THEN
        IF (var%un_disk /= var%un_disk) WRITE(*, *) "localvar%un_disk is IDENTICAL (both NaN, uninitialized)." 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%un_disk is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          diff_un_disk = ABS(var%un_disk - kgenref_var%un_disk) 
          IF (diff_un_disk <= kgen_tolerance) THEN 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%un_disk is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          ELSE 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%un_disk is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_un_disk 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) "Difference is ", diff_un_disk 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (ALL(var%v0 == kgenref_var%v0)) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%v0 is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          ALLOCATE (buf1_v0(SIZE(var%v0,dim=1))) 
          ALLOCATE (buf2_v0(SIZE(var%v0,dim=1))) 
          n_v0 = COUNT(var%v0 /= kgenref_var%v0) 
          WHERE ( ABS(kgenref_var%v0) > kgen_minvalue ) 
              buf1_v0 = ((var%v0-kgenref_var%v0)/kgenref_var%v0)**2 
              buf2_v0 = (var%v0-kgenref_var%v0)**2 
          ELSEWHERE 
              buf1_v0 = (var%v0-kgenref_var%v0)**2 
              buf2_v0 = buf1_v0 
          END WHERE   
          nrmsdiff_v0 = SQRT(SUM(buf1_v0)/REAL(n_v0)) 
          rmsdiff_v0 = SQRT(SUM(buf2_v0)/REAL(n_v0)) 
          IF (rmsdiff_v0 > kgen_tolerance) THEN 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%v0 is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          ELSE 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%v0 is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) count( var%v0 /= kgenref_var%v0), " of ", size( var%v0 ), " elements are different." 
                  WRITE (*, *) "Average - kernel ", sum(var%v0)/real(size(var%v0)) 
                  WRITE (*, *) "Average - reference ", sum(kgenref_var%v0)/real(size(kgenref_var%v0)) 
                  WRITE (*, *) "RMS of difference is ", rmsdiff_v0 
                  WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_v0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) count( var%v0 /= kgenref_var%v0), " of ", size( var%v0 ), " elements are different." 
                  WRITE (*, *) "Average - kernel ", sum(var%v0)/real(size(var%v0)) 
                  WRITE (*, *) "Average - reference ", sum(kgenref_var%v0)/real(size(kgenref_var%v0)) 
                  WRITE (*, *) "RMS of difference is ", rmsdiff_v0 
                  WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_v0 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
      IF (ALL(var%x_hat_disk == kgenref_var%x_hat_disk)) THEN 
          dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank == 0) THEN 
                  WRITE (*, *) trim(adjustl(varname)), "%x_hat_disk is IDENTICAL." 
              END IF   
          END IF   
          check_result = CHECK_IDENTICAL 
      ELSE 
          ALLOCATE (buf1_x_hat_disk(SIZE(var%x_hat_disk,dim=1))) 
          ALLOCATE (buf2_x_hat_disk(SIZE(var%x_hat_disk,dim=1))) 
          n_x_hat_disk = COUNT(var%x_hat_disk /= kgenref_var%x_hat_disk) 
          WHERE ( ABS(kgenref_var%x_hat_disk) > kgen_minvalue ) 
              buf1_x_hat_disk = ((var%x_hat_disk-kgenref_var%x_hat_disk)/kgenref_var%x_hat_disk)**2 
              buf2_x_hat_disk = (var%x_hat_disk-kgenref_var%x_hat_disk)**2 
          ELSEWHERE 
              buf1_x_hat_disk = (var%x_hat_disk-kgenref_var%x_hat_disk)**2 
              buf2_x_hat_disk = buf1_x_hat_disk 
          END WHERE   
          nrmsdiff_x_hat_disk = SQRT(SUM(buf1_x_hat_disk)/REAL(n_x_hat_disk)) 
          rmsdiff_x_hat_disk = SQRT(SUM(buf2_x_hat_disk)/REAL(n_x_hat_disk)) 
          IF (rmsdiff_x_hat_disk > kgen_tolerance) THEN 
              dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%x_hat_disk is NOT IDENTICAL(out of tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_OUT_TOL 
          ELSE 
              dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
              IF (kgen_verboseLevel > 1) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%x_hat_disk is NOT IDENTICAL(within tolerance)." 
                  END IF   
              END IF   
              check_result = CHECK_IN_TOL 
          END IF   
      END IF   
      IF (check_result == CHECK_IDENTICAL) THEN 
          CONTINUE 
      ELSE IF (check_result == CHECK_OUT_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) count( var%x_hat_disk /= kgenref_var%x_hat_disk), " of ", size( var%x_hat_disk ), " elements are &
                  &different." 
                  WRITE (*, *) "Average - kernel ", sum(var%x_hat_disk)/real(size(var%x_hat_disk)) 
                  WRITE (*, *) "Average - reference ", sum(kgenref_var%x_hat_disk)/real(size(kgenref_var%x_hat_disk)) 
                  WRITE (*, *) "RMS of difference is ", rmsdiff_x_hat_disk 
                  WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_x_hat_disk 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      ELSE IF (check_result == CHECK_IN_TOL) THEN 
          IF (kgen_verboseLevel > 2) THEN 
              IF (check_status%rank ==0) THEN 
                  WRITE (*, *) count( var%x_hat_disk /= kgenref_var%x_hat_disk), " of ", size( var%x_hat_disk ), " elements are &
                  &different." 
                  WRITE (*, *) "Average - kernel ", sum(var%x_hat_disk)/real(size(var%x_hat_disk)) 
                  WRITE (*, *) "Average - reference ", sum(kgenref_var%x_hat_disk)/real(size(kgenref_var%x_hat_disk)) 
                  WRITE (*, *) "RMS of difference is ", rmsdiff_x_hat_disk 
                  WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_x_hat_disk 
                  WRITE (*, *) "" 
              END IF   
          END IF   
      END IF   
        
      IF (ALLOCATED(var%userprop)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%userprop == kgenref_var%userprop)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%userprop is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_userprop(SIZE(var%userprop,dim=1),SIZE(var%userprop,dim=2))) 
              ALLOCATE (buf2_userprop(SIZE(var%userprop,dim=1),SIZE(var%userprop,dim=2))) 
              n_userprop = COUNT(var%userprop /= kgenref_var%userprop) 
              WHERE ( ABS(kgenref_var%userprop) > kgen_minvalue ) 
                  buf1_userprop = ((var%userprop-kgenref_var%userprop)/kgenref_var%userprop)**2 
                  buf2_userprop = (var%userprop-kgenref_var%userprop)**2 
              ELSEWHERE 
                  buf1_userprop = (var%userprop-kgenref_var%userprop)**2 
                  buf2_userprop = buf1_userprop 
              END WHERE   
              nrmsdiff_userprop = SQRT(SUM(buf1_userprop)/REAL(n_userprop)) 
              rmsdiff_userprop = SQRT(SUM(buf2_userprop)/REAL(n_userprop)) 
              IF (rmsdiff_userprop > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%userprop is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%userprop is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%userprop /= kgenref_var%userprop), " of ", size( var%userprop ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%userprop)/real(size(var%userprop)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%userprop)/real(size(kgenref_var%userprop)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_userprop 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_userprop 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%userprop /= kgenref_var%userprop), " of ", size( var%userprop ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%userprop)/real(size(var%userprop)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%userprop)/real(size(kgenref_var%userprop)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_userprop 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_userprop 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cantangle)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cantangle == kgenref_var%cantangle)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cantangle is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cantangle(SIZE(var%cantangle,dim=1),SIZE(var%cantangle,dim=2))) 
              ALLOCATE (buf2_cantangle(SIZE(var%cantangle,dim=1),SIZE(var%cantangle,dim=2))) 
              n_cantangle = COUNT(var%cantangle /= kgenref_var%cantangle) 
              WHERE ( ABS(kgenref_var%cantangle) > kgen_minvalue ) 
                  buf1_cantangle = ((var%cantangle-kgenref_var%cantangle)/kgenref_var%cantangle)**2 
                  buf2_cantangle = (var%cantangle-kgenref_var%cantangle)**2 
              ELSEWHERE 
                  buf1_cantangle = (var%cantangle-kgenref_var%cantangle)**2 
                  buf2_cantangle = buf1_cantangle 
              END WHERE   
              nrmsdiff_cantangle = SQRT(SUM(buf1_cantangle)/REAL(n_cantangle)) 
              rmsdiff_cantangle = SQRT(SUM(buf2_cantangle)/REAL(n_cantangle)) 
              IF (rmsdiff_cantangle > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cantangle is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cantangle is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cantangle /= kgenref_var%cantangle), " of ", size( var%cantangle ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cantangle)/real(size(var%cantangle)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cantangle)/real(size(kgenref_var%cantangle)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cantangle 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cantangle 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cantangle /= kgenref_var%cantangle), " of ", size( var%cantangle ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cantangle)/real(size(var%cantangle)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cantangle)/real(size(kgenref_var%cantangle)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cantangle 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cantangle 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%drdz)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%drdz == kgenref_var%drdz)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%drdz is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_drdz(SIZE(var%drdz,dim=1),SIZE(var%drdz,dim=2))) 
              ALLOCATE (buf2_drdz(SIZE(var%drdz,dim=1),SIZE(var%drdz,dim=2))) 
              n_drdz = COUNT(var%drdz /= kgenref_var%drdz) 
              WHERE ( ABS(kgenref_var%drdz) > kgen_minvalue ) 
                  buf1_drdz = ((var%drdz-kgenref_var%drdz)/kgenref_var%drdz)**2 
                  buf2_drdz = (var%drdz-kgenref_var%drdz)**2 
              ELSEWHERE 
                  buf1_drdz = (var%drdz-kgenref_var%drdz)**2 
                  buf2_drdz = buf1_drdz 
              END WHERE   
              nrmsdiff_drdz = SQRT(SUM(buf1_drdz)/REAL(n_drdz)) 
              rmsdiff_drdz = SQRT(SUM(buf2_drdz)/REAL(n_drdz)) 
              IF (rmsdiff_drdz > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%drdz is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%drdz is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%drdz /= kgenref_var%drdz), " of ", size( var%drdz ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%drdz)/real(size(var%drdz)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%drdz)/real(size(kgenref_var%drdz)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_drdz 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_drdz 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%drdz /= kgenref_var%drdz), " of ", size( var%drdz ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%drdz)/real(size(var%drdz)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%drdz)/real(size(kgenref_var%drdz)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_drdz 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_drdz 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%toeangle)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%toeangle == kgenref_var%toeangle)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%toeangle is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_toeangle(SIZE(var%toeangle,dim=1),SIZE(var%toeangle,dim=2))) 
              ALLOCATE (buf2_toeangle(SIZE(var%toeangle,dim=1),SIZE(var%toeangle,dim=2))) 
              n_toeangle = COUNT(var%toeangle /= kgenref_var%toeangle) 
              WHERE ( ABS(kgenref_var%toeangle) > kgen_minvalue ) 
                  buf1_toeangle = ((var%toeangle-kgenref_var%toeangle)/kgenref_var%toeangle)**2 
                  buf2_toeangle = (var%toeangle-kgenref_var%toeangle)**2 
              ELSEWHERE 
                  buf1_toeangle = (var%toeangle-kgenref_var%toeangle)**2 
                  buf2_toeangle = buf1_toeangle 
              END WHERE   
              nrmsdiff_toeangle = SQRT(SUM(buf1_toeangle)/REAL(n_toeangle)) 
              rmsdiff_toeangle = SQRT(SUM(buf2_toeangle)/REAL(n_toeangle)) 
              IF (rmsdiff_toeangle > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%toeangle is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%toeangle is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%toeangle /= kgenref_var%toeangle), " of ", size( var%toeangle ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%toeangle)/real(size(var%toeangle)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%toeangle)/real(size(kgenref_var%toeangle)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_toeangle 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_toeangle 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%toeangle /= kgenref_var%toeangle), " of ", size( var%toeangle ), " elements are &
                      &different." 
                      WRITE (*, *) "Average - kernel ", sum(var%toeangle)/real(size(var%toeangle)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%toeangle)/real(size(kgenref_var%toeangle)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_toeangle 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_toeangle 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (dtype_check_status%numTotal == dtype_check_status%numIdentical) THEN 
          check_status%numIdentical = check_status%numIdentical + 1 
      ELSE IF (dtype_check_status%numOutTol > 0) THEN 
          check_status%numOutTol = check_status%numOutTol + 1 
      ELSE IF (dtype_check_status%numInTol > 0) THEN 
          check_status%numInTol = check_status%numInTol + 1 
      END IF   
  END SUBROUTINE kv_bemt_types_bemt_inputtype 
    
  !verify state subroutine for kv_bemt_types_bemt_outputtype 
  RECURSIVE SUBROUTINE kv_bemt_types_bemt_outputtype(varname, check_status, var, kgenref_var) 
      CHARACTER(LEN=*), INTENT(IN) :: varname 
      TYPE(check_t), INTENT(INOUT) :: check_status 
      TYPE(bemt_outputtype), INTENT(IN) :: var, kgenref_var 
      TYPE(check_t) :: dtype_check_status, comp_check_status 
      INTEGER :: check_result 
      LOGICAL :: is_print = .FALSE. 
        
      INTEGER :: n_vrel 
      real(KIND=reki) :: nrmsdiff_vrel, rmsdiff_vrel 
      real(KIND=reki), ALLOCATABLE :: buf1_vrel(:,:), buf2_vrel(:,:) 
      INTEGER :: n_phi 
      real(KIND=reki) :: nrmsdiff_phi, rmsdiff_phi 
      real(KIND=reki), ALLOCATABLE :: buf1_phi(:,:), buf2_phi(:,:) 
      INTEGER :: n_axinduction 
      real(KIND=reki) :: nrmsdiff_axinduction, rmsdiff_axinduction 
      real(KIND=reki), ALLOCATABLE :: buf1_axinduction(:,:), buf2_axinduction(:,:) 
      INTEGER :: n_taninduction 
      real(KIND=reki) :: nrmsdiff_taninduction, rmsdiff_taninduction 
      real(KIND=reki), ALLOCATABLE :: buf1_taninduction(:,:), buf2_taninduction(:,:) 
      INTEGER :: n_axinduction_qs 
      real(KIND=reki) :: nrmsdiff_axinduction_qs, rmsdiff_axinduction_qs 
      real(KIND=reki), ALLOCATABLE :: buf1_axinduction_qs(:,:), buf2_axinduction_qs(:,:) 
      INTEGER :: n_taninduction_qs 
      real(KIND=reki) :: nrmsdiff_taninduction_qs, rmsdiff_taninduction_qs 
      real(KIND=reki), ALLOCATABLE :: buf1_taninduction_qs(:,:), buf2_taninduction_qs(:,:) 
      INTEGER :: n_k 
      real(KIND=reki) :: nrmsdiff_k, rmsdiff_k 
      real(KIND=reki), ALLOCATABLE :: buf1_k(:,:), buf2_k(:,:) 
      INTEGER :: n_k_p 
      real(KIND=reki) :: nrmsdiff_k_p, rmsdiff_k_p 
      real(KIND=reki), ALLOCATABLE :: buf1_k_p(:,:), buf2_k_p(:,:) 
      INTEGER :: n_f 
      real(KIND=reki) :: nrmsdiff_f, rmsdiff_f 
      real(KIND=reki), ALLOCATABLE :: buf1_f(:,:), buf2_f(:,:) 
      INTEGER :: n_re 
      real(KIND=reki) :: nrmsdiff_re, rmsdiff_re 
      real(KIND=reki), ALLOCATABLE :: buf1_re(:,:), buf2_re(:,:) 
      INTEGER :: n_aoa 
      real(KIND=reki) :: nrmsdiff_aoa, rmsdiff_aoa 
      real(KIND=reki), ALLOCATABLE :: buf1_aoa(:,:), buf2_aoa(:,:) 
      INTEGER :: n_cx 
      real(KIND=reki) :: nrmsdiff_cx, rmsdiff_cx 
      real(KIND=reki), ALLOCATABLE :: buf1_cx(:,:), buf2_cx(:,:) 
      INTEGER :: n_cy 
      real(KIND=reki) :: nrmsdiff_cy, rmsdiff_cy 
      real(KIND=reki), ALLOCATABLE :: buf1_cy(:,:), buf2_cy(:,:) 
      INTEGER :: n_cz 
      real(KIND=reki) :: nrmsdiff_cz, rmsdiff_cz 
      real(KIND=reki), ALLOCATABLE :: buf1_cz(:,:), buf2_cz(:,:) 
      INTEGER :: n_cmx 
      real(KIND=reki) :: nrmsdiff_cmx, rmsdiff_cmx 
      real(KIND=reki), ALLOCATABLE :: buf1_cmx(:,:), buf2_cmx(:,:) 
      INTEGER :: n_cmy 
      real(KIND=reki) :: nrmsdiff_cmy, rmsdiff_cmy 
      real(KIND=reki), ALLOCATABLE :: buf1_cmy(:,:), buf2_cmy(:,:) 
      INTEGER :: n_cmz 
      real(KIND=reki) :: nrmsdiff_cmz, rmsdiff_cmz 
      real(KIND=reki), ALLOCATABLE :: buf1_cmz(:,:), buf2_cmz(:,:) 
      INTEGER :: n_cm 
      real(KIND=reki) :: nrmsdiff_cm, rmsdiff_cm 
      real(KIND=reki), ALLOCATABLE :: buf1_cm(:,:), buf2_cm(:,:) 
      INTEGER :: n_cl 
      real(KIND=reki) :: nrmsdiff_cl, rmsdiff_cl 
      real(KIND=reki), ALLOCATABLE :: buf1_cl(:,:), buf2_cl(:,:) 
      INTEGER :: n_cd 
      real(KIND=reki) :: nrmsdiff_cd, rmsdiff_cd 
      real(KIND=reki), ALLOCATABLE :: buf1_cd(:,:), buf2_cd(:,:) 
      INTEGER :: n_chi 
      real(KIND=reki) :: nrmsdiff_chi, rmsdiff_chi 
      real(KIND=reki), ALLOCATABLE :: buf1_chi(:,:), buf2_chi(:,:) 
      INTEGER :: n_cpmin 
      real(KIND=reki) :: nrmsdiff_cpmin, rmsdiff_cpmin 
      real(KIND=reki), ALLOCATABLE :: buf1_cpmin(:,:), buf2_cpmin(:,:) 
        
      check_status%numTotal = check_status%numTotal + 1 
        
      CALL kgen_init_check(dtype_check_status, rank=check_status%rank) 
      IF (ALLOCATED(var%vrel)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%vrel == kgenref_var%vrel)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%vrel is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_vrel(SIZE(var%vrel,dim=1),SIZE(var%vrel,dim=2))) 
              ALLOCATE (buf2_vrel(SIZE(var%vrel,dim=1),SIZE(var%vrel,dim=2))) 
              n_vrel = COUNT(var%vrel /= kgenref_var%vrel) 
              WHERE ( ABS(kgenref_var%vrel) > kgen_minvalue ) 
                  buf1_vrel = ((var%vrel-kgenref_var%vrel)/kgenref_var%vrel)**2 
                  buf2_vrel = (var%vrel-kgenref_var%vrel)**2 
              ELSEWHERE 
                  buf1_vrel = (var%vrel-kgenref_var%vrel)**2 
                  buf2_vrel = buf1_vrel 
              END WHERE   
              nrmsdiff_vrel = SQRT(SUM(buf1_vrel)/REAL(n_vrel)) 
              rmsdiff_vrel = SQRT(SUM(buf2_vrel)/REAL(n_vrel)) 
              IF (rmsdiff_vrel > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%vrel is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%vrel is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%vrel /= kgenref_var%vrel), " of ", size( var%vrel ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%vrel)/real(size(var%vrel)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%vrel)/real(size(kgenref_var%vrel)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_vrel 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_vrel 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%vrel /= kgenref_var%vrel), " of ", size( var%vrel ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%vrel)/real(size(var%vrel)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%vrel)/real(size(kgenref_var%vrel)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_vrel 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_vrel 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%phi)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%phi == kgenref_var%phi)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%phi is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_phi(SIZE(var%phi,dim=1),SIZE(var%phi,dim=2))) 
              ALLOCATE (buf2_phi(SIZE(var%phi,dim=1),SIZE(var%phi,dim=2))) 
              n_phi = COUNT(var%phi /= kgenref_var%phi) 
              WHERE ( ABS(kgenref_var%phi) > kgen_minvalue ) 
                  buf1_phi = ((var%phi-kgenref_var%phi)/kgenref_var%phi)**2 
                  buf2_phi = (var%phi-kgenref_var%phi)**2 
              ELSEWHERE 
                  buf1_phi = (var%phi-kgenref_var%phi)**2 
                  buf2_phi = buf1_phi 
              END WHERE   
              nrmsdiff_phi = SQRT(SUM(buf1_phi)/REAL(n_phi)) 
              rmsdiff_phi = SQRT(SUM(buf2_phi)/REAL(n_phi)) 
              IF (rmsdiff_phi > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%phi is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%phi is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%phi /= kgenref_var%phi), " of ", size( var%phi ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%phi)/real(size(var%phi)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%phi)/real(size(kgenref_var%phi)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_phi 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_phi 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%phi /= kgenref_var%phi), " of ", size( var%phi ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%phi)/real(size(var%phi)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%phi)/real(size(kgenref_var%phi)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_phi 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_phi 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%axinduction)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%axinduction == kgenref_var%axinduction)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%axinduction is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_axinduction(SIZE(var%axinduction,dim=1),SIZE(var%axinduction,dim=2))) 
              ALLOCATE (buf2_axinduction(SIZE(var%axinduction,dim=1),SIZE(var%axinduction,dim=2))) 
              n_axinduction = COUNT(var%axinduction /= kgenref_var%axinduction) 
              WHERE ( ABS(kgenref_var%axinduction) > kgen_minvalue ) 
                  buf1_axinduction = ((var%axinduction-kgenref_var%axinduction)/kgenref_var%axinduction)**2 
                  buf2_axinduction = (var%axinduction-kgenref_var%axinduction)**2 
              ELSEWHERE 
                  buf1_axinduction = (var%axinduction-kgenref_var%axinduction)**2 
                  buf2_axinduction = buf1_axinduction 
              END WHERE   
              nrmsdiff_axinduction = SQRT(SUM(buf1_axinduction)/REAL(n_axinduction)) 
              rmsdiff_axinduction = SQRT(SUM(buf2_axinduction)/REAL(n_axinduction)) 
              IF (rmsdiff_axinduction > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%axinduction is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%axinduction is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%axinduction /= kgenref_var%axinduction), " of ", size( var%axinduction ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%axinduction)/real(size(var%axinduction)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%axinduction)/real(size(kgenref_var%axinduction)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_axinduction 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_axinduction 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%axinduction /= kgenref_var%axinduction), " of ", size( var%axinduction ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%axinduction)/real(size(var%axinduction)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%axinduction)/real(size(kgenref_var%axinduction)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_axinduction 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_axinduction 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%taninduction)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%taninduction == kgenref_var%taninduction)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%taninduction is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_taninduction(SIZE(var%taninduction,dim=1),SIZE(var%taninduction,dim=2))) 
              ALLOCATE (buf2_taninduction(SIZE(var%taninduction,dim=1),SIZE(var%taninduction,dim=2))) 
              n_taninduction = COUNT(var%taninduction /= kgenref_var%taninduction) 
              WHERE ( ABS(kgenref_var%taninduction) > kgen_minvalue ) 
                  buf1_taninduction = ((var%taninduction-kgenref_var%taninduction)/kgenref_var%taninduction)**2 
                  buf2_taninduction = (var%taninduction-kgenref_var%taninduction)**2 
              ELSEWHERE 
                  buf1_taninduction = (var%taninduction-kgenref_var%taninduction)**2 
                  buf2_taninduction = buf1_taninduction 
              END WHERE   
              nrmsdiff_taninduction = SQRT(SUM(buf1_taninduction)/REAL(n_taninduction)) 
              rmsdiff_taninduction = SQRT(SUM(buf2_taninduction)/REAL(n_taninduction)) 
              IF (rmsdiff_taninduction > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%taninduction is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%taninduction is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%taninduction /= kgenref_var%taninduction), " of ", size( var%taninduction ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%taninduction)/real(size(var%taninduction)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%taninduction)/real(size(kgenref_var%taninduction)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_taninduction 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_taninduction 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%taninduction /= kgenref_var%taninduction), " of ", size( var%taninduction ), " &
                      &elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%taninduction)/real(size(var%taninduction)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%taninduction)/real(size(kgenref_var%taninduction)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_taninduction 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_taninduction 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%axinduction_qs)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%axinduction_qs == kgenref_var%axinduction_qs)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%axinduction_qs is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_axinduction_qs(SIZE(var%axinduction_qs,dim=1),SIZE(var%axinduction_qs,dim=2))) 
              ALLOCATE (buf2_axinduction_qs(SIZE(var%axinduction_qs,dim=1),SIZE(var%axinduction_qs,dim=2))) 
              n_axinduction_qs = COUNT(var%axinduction_qs /= kgenref_var%axinduction_qs) 
              WHERE ( ABS(kgenref_var%axinduction_qs) > kgen_minvalue ) 
                  buf1_axinduction_qs = ((var%axinduction_qs-kgenref_var%axinduction_qs)/kgenref_var%axinduction_qs)**2 
                  buf2_axinduction_qs = (var%axinduction_qs-kgenref_var%axinduction_qs)**2 
              ELSEWHERE 
                  buf1_axinduction_qs = (var%axinduction_qs-kgenref_var%axinduction_qs)**2 
                  buf2_axinduction_qs = buf1_axinduction_qs 
              END WHERE   
              nrmsdiff_axinduction_qs = SQRT(SUM(buf1_axinduction_qs)/REAL(n_axinduction_qs)) 
              rmsdiff_axinduction_qs = SQRT(SUM(buf2_axinduction_qs)/REAL(n_axinduction_qs)) 
              IF (rmsdiff_axinduction_qs > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%axinduction_qs is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%axinduction_qs is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%axinduction_qs /= kgenref_var%axinduction_qs), " of ", size( var%axinduction_qs ), &
                      &" elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%axinduction_qs)/real(size(var%axinduction_qs)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%axinduction_qs)/real(size(kgenref_var%axinduction_qs)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_axinduction_qs 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_axinduction_qs 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%axinduction_qs /= kgenref_var%axinduction_qs), " of ", size( var%axinduction_qs ), &
                      &" elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%axinduction_qs)/real(size(var%axinduction_qs)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%axinduction_qs)/real(size(kgenref_var%axinduction_qs)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_axinduction_qs 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_axinduction_qs 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%taninduction_qs)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%taninduction_qs == kgenref_var%taninduction_qs)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%taninduction_qs is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_taninduction_qs(SIZE(var%taninduction_qs,dim=1),SIZE(var%taninduction_qs,dim=2))) 
              ALLOCATE (buf2_taninduction_qs(SIZE(var%taninduction_qs,dim=1),SIZE(var%taninduction_qs,dim=2))) 
              n_taninduction_qs = COUNT(var%taninduction_qs /= kgenref_var%taninduction_qs) 
              WHERE ( ABS(kgenref_var%taninduction_qs) > kgen_minvalue ) 
                  buf1_taninduction_qs = ((var%taninduction_qs-kgenref_var%taninduction_qs)/kgenref_var%taninduction_qs)**2 
                  buf2_taninduction_qs = (var%taninduction_qs-kgenref_var%taninduction_qs)**2 
              ELSEWHERE 
                  buf1_taninduction_qs = (var%taninduction_qs-kgenref_var%taninduction_qs)**2 
                  buf2_taninduction_qs = buf1_taninduction_qs 
              END WHERE   
              nrmsdiff_taninduction_qs = SQRT(SUM(buf1_taninduction_qs)/REAL(n_taninduction_qs)) 
              rmsdiff_taninduction_qs = SQRT(SUM(buf2_taninduction_qs)/REAL(n_taninduction_qs)) 
              IF (rmsdiff_taninduction_qs > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%taninduction_qs is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%taninduction_qs is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%taninduction_qs /= kgenref_var%taninduction_qs), " of ", size( var%taninduction_qs &
                      &), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%taninduction_qs)/real(size(var%taninduction_qs)) 
                      WRITE (*, *) "Average - reference ", &
                      &sum(kgenref_var%taninduction_qs)/real(size(kgenref_var%taninduction_qs)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_taninduction_qs 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_taninduction_qs 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%taninduction_qs /= kgenref_var%taninduction_qs), " of ", size( var%taninduction_qs &
                      &), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%taninduction_qs)/real(size(var%taninduction_qs)) 
                      WRITE (*, *) "Average - reference ", &
                      &sum(kgenref_var%taninduction_qs)/real(size(kgenref_var%taninduction_qs)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_taninduction_qs 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_taninduction_qs 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%k)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%k == kgenref_var%k)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_k(SIZE(var%k,dim=1),SIZE(var%k,dim=2))) 
              ALLOCATE (buf2_k(SIZE(var%k,dim=1),SIZE(var%k,dim=2))) 
              n_k = COUNT(var%k /= kgenref_var%k) 
              WHERE ( ABS(kgenref_var%k) > kgen_minvalue ) 
                  buf1_k = ((var%k-kgenref_var%k)/kgenref_var%k)**2 
                  buf2_k = (var%k-kgenref_var%k)**2 
              ELSEWHERE 
                  buf1_k = (var%k-kgenref_var%k)**2 
                  buf2_k = buf1_k 
              END WHERE   
              nrmsdiff_k = SQRT(SUM(buf1_k)/REAL(n_k)) 
              rmsdiff_k = SQRT(SUM(buf2_k)/REAL(n_k)) 
              IF (rmsdiff_k > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%k is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%k is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%k /= kgenref_var%k), " of ", size( var%k ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%k)/real(size(var%k)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%k)/real(size(kgenref_var%k)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_k 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_k 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%k /= kgenref_var%k), " of ", size( var%k ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%k)/real(size(var%k)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%k)/real(size(kgenref_var%k)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_k 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_k 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%k_p)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%k_p == kgenref_var%k_p)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%k_p is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_k_p(SIZE(var%k_p,dim=1),SIZE(var%k_p,dim=2))) 
              ALLOCATE (buf2_k_p(SIZE(var%k_p,dim=1),SIZE(var%k_p,dim=2))) 
              n_k_p = COUNT(var%k_p /= kgenref_var%k_p) 
              WHERE ( ABS(kgenref_var%k_p) > kgen_minvalue ) 
                  buf1_k_p = ((var%k_p-kgenref_var%k_p)/kgenref_var%k_p)**2 
                  buf2_k_p = (var%k_p-kgenref_var%k_p)**2 
              ELSEWHERE 
                  buf1_k_p = (var%k_p-kgenref_var%k_p)**2 
                  buf2_k_p = buf1_k_p 
              END WHERE   
              nrmsdiff_k_p = SQRT(SUM(buf1_k_p)/REAL(n_k_p)) 
              rmsdiff_k_p = SQRT(SUM(buf2_k_p)/REAL(n_k_p)) 
              IF (rmsdiff_k_p > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%k_p is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%k_p is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%k_p /= kgenref_var%k_p), " of ", size( var%k_p ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%k_p)/real(size(var%k_p)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%k_p)/real(size(kgenref_var%k_p)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_k_p 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_k_p 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%k_p /= kgenref_var%k_p), " of ", size( var%k_p ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%k_p)/real(size(var%k_p)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%k_p)/real(size(kgenref_var%k_p)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_k_p 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_k_p 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%f)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%f == kgenref_var%f)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%f is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_f(SIZE(var%f,dim=1),SIZE(var%f,dim=2))) 
              ALLOCATE (buf2_f(SIZE(var%f,dim=1),SIZE(var%f,dim=2))) 
              n_f = COUNT(var%f /= kgenref_var%f) 
              WHERE ( ABS(kgenref_var%f) > kgen_minvalue ) 
                  buf1_f = ((var%f-kgenref_var%f)/kgenref_var%f)**2 
                  buf2_f = (var%f-kgenref_var%f)**2 
              ELSEWHERE 
                  buf1_f = (var%f-kgenref_var%f)**2 
                  buf2_f = buf1_f 
              END WHERE   
              nrmsdiff_f = SQRT(SUM(buf1_f)/REAL(n_f)) 
              rmsdiff_f = SQRT(SUM(buf2_f)/REAL(n_f)) 
              IF (rmsdiff_f > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%f is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%f is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%f /= kgenref_var%f), " of ", size( var%f ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%f)/real(size(var%f)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%f)/real(size(kgenref_var%f)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_f 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_f 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%f /= kgenref_var%f), " of ", size( var%f ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%f)/real(size(var%f)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%f)/real(size(kgenref_var%f)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_f 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_f 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%re)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%re == kgenref_var%re)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%re is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_re(SIZE(var%re,dim=1),SIZE(var%re,dim=2))) 
              ALLOCATE (buf2_re(SIZE(var%re,dim=1),SIZE(var%re,dim=2))) 
              n_re = COUNT(var%re /= kgenref_var%re) 
              WHERE ( ABS(kgenref_var%re) > kgen_minvalue ) 
                  buf1_re = ((var%re-kgenref_var%re)/kgenref_var%re)**2 
                  buf2_re = (var%re-kgenref_var%re)**2 
              ELSEWHERE 
                  buf1_re = (var%re-kgenref_var%re)**2 
                  buf2_re = buf1_re 
              END WHERE   
              nrmsdiff_re = SQRT(SUM(buf1_re)/REAL(n_re)) 
              rmsdiff_re = SQRT(SUM(buf2_re)/REAL(n_re)) 
              IF (rmsdiff_re > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%re is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%re is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%re /= kgenref_var%re), " of ", size( var%re ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%re)/real(size(var%re)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%re)/real(size(kgenref_var%re)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_re 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_re 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%re /= kgenref_var%re), " of ", size( var%re ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%re)/real(size(var%re)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%re)/real(size(kgenref_var%re)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_re 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_re 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%aoa)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%aoa == kgenref_var%aoa)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%aoa is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_aoa(SIZE(var%aoa,dim=1),SIZE(var%aoa,dim=2))) 
              ALLOCATE (buf2_aoa(SIZE(var%aoa,dim=1),SIZE(var%aoa,dim=2))) 
              n_aoa = COUNT(var%aoa /= kgenref_var%aoa) 
              WHERE ( ABS(kgenref_var%aoa) > kgen_minvalue ) 
                  buf1_aoa = ((var%aoa-kgenref_var%aoa)/kgenref_var%aoa)**2 
                  buf2_aoa = (var%aoa-kgenref_var%aoa)**2 
              ELSEWHERE 
                  buf1_aoa = (var%aoa-kgenref_var%aoa)**2 
                  buf2_aoa = buf1_aoa 
              END WHERE   
              nrmsdiff_aoa = SQRT(SUM(buf1_aoa)/REAL(n_aoa)) 
              rmsdiff_aoa = SQRT(SUM(buf2_aoa)/REAL(n_aoa)) 
              IF (rmsdiff_aoa > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%aoa is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%aoa is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%aoa /= kgenref_var%aoa), " of ", size( var%aoa ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%aoa)/real(size(var%aoa)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%aoa)/real(size(kgenref_var%aoa)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_aoa 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_aoa 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%aoa /= kgenref_var%aoa), " of ", size( var%aoa ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%aoa)/real(size(var%aoa)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%aoa)/real(size(kgenref_var%aoa)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_aoa 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_aoa 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cx)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cx == kgenref_var%cx)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cx is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cx(SIZE(var%cx,dim=1),SIZE(var%cx,dim=2))) 
              ALLOCATE (buf2_cx(SIZE(var%cx,dim=1),SIZE(var%cx,dim=2))) 
              n_cx = COUNT(var%cx /= kgenref_var%cx) 
              WHERE ( ABS(kgenref_var%cx) > kgen_minvalue ) 
                  buf1_cx = ((var%cx-kgenref_var%cx)/kgenref_var%cx)**2 
                  buf2_cx = (var%cx-kgenref_var%cx)**2 
              ELSEWHERE 
                  buf1_cx = (var%cx-kgenref_var%cx)**2 
                  buf2_cx = buf1_cx 
              END WHERE   
              nrmsdiff_cx = SQRT(SUM(buf1_cx)/REAL(n_cx)) 
              rmsdiff_cx = SQRT(SUM(buf2_cx)/REAL(n_cx)) 
              IF (rmsdiff_cx > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cx is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cx is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cx /= kgenref_var%cx), " of ", size( var%cx ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cx)/real(size(var%cx)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cx)/real(size(kgenref_var%cx)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cx 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cx 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cx /= kgenref_var%cx), " of ", size( var%cx ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cx)/real(size(var%cx)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cx)/real(size(kgenref_var%cx)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cx 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cx 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cy)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cy == kgenref_var%cy)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cy is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cy(SIZE(var%cy,dim=1),SIZE(var%cy,dim=2))) 
              ALLOCATE (buf2_cy(SIZE(var%cy,dim=1),SIZE(var%cy,dim=2))) 
              n_cy = COUNT(var%cy /= kgenref_var%cy) 
              WHERE ( ABS(kgenref_var%cy) > kgen_minvalue ) 
                  buf1_cy = ((var%cy-kgenref_var%cy)/kgenref_var%cy)**2 
                  buf2_cy = (var%cy-kgenref_var%cy)**2 
              ELSEWHERE 
                  buf1_cy = (var%cy-kgenref_var%cy)**2 
                  buf2_cy = buf1_cy 
              END WHERE   
              nrmsdiff_cy = SQRT(SUM(buf1_cy)/REAL(n_cy)) 
              rmsdiff_cy = SQRT(SUM(buf2_cy)/REAL(n_cy)) 
              IF (rmsdiff_cy > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cy is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cy is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cy /= kgenref_var%cy), " of ", size( var%cy ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cy)/real(size(var%cy)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cy)/real(size(kgenref_var%cy)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cy 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cy 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cy /= kgenref_var%cy), " of ", size( var%cy ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cy)/real(size(var%cy)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cy)/real(size(kgenref_var%cy)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cy 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cy 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cz)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cz == kgenref_var%cz)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cz is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cz(SIZE(var%cz,dim=1),SIZE(var%cz,dim=2))) 
              ALLOCATE (buf2_cz(SIZE(var%cz,dim=1),SIZE(var%cz,dim=2))) 
              n_cz = COUNT(var%cz /= kgenref_var%cz) 
              WHERE ( ABS(kgenref_var%cz) > kgen_minvalue ) 
                  buf1_cz = ((var%cz-kgenref_var%cz)/kgenref_var%cz)**2 
                  buf2_cz = (var%cz-kgenref_var%cz)**2 
              ELSEWHERE 
                  buf1_cz = (var%cz-kgenref_var%cz)**2 
                  buf2_cz = buf1_cz 
              END WHERE   
              nrmsdiff_cz = SQRT(SUM(buf1_cz)/REAL(n_cz)) 
              rmsdiff_cz = SQRT(SUM(buf2_cz)/REAL(n_cz)) 
              IF (rmsdiff_cz > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cz is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cz is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cz /= kgenref_var%cz), " of ", size( var%cz ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cz)/real(size(var%cz)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cz)/real(size(kgenref_var%cz)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cz 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cz 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cz /= kgenref_var%cz), " of ", size( var%cz ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cz)/real(size(var%cz)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cz)/real(size(kgenref_var%cz)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cz 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cz 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cmx)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cmx == kgenref_var%cmx)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cmx is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cmx(SIZE(var%cmx,dim=1),SIZE(var%cmx,dim=2))) 
              ALLOCATE (buf2_cmx(SIZE(var%cmx,dim=1),SIZE(var%cmx,dim=2))) 
              n_cmx = COUNT(var%cmx /= kgenref_var%cmx) 
              WHERE ( ABS(kgenref_var%cmx) > kgen_minvalue ) 
                  buf1_cmx = ((var%cmx-kgenref_var%cmx)/kgenref_var%cmx)**2 
                  buf2_cmx = (var%cmx-kgenref_var%cmx)**2 
              ELSEWHERE 
                  buf1_cmx = (var%cmx-kgenref_var%cmx)**2 
                  buf2_cmx = buf1_cmx 
              END WHERE   
              nrmsdiff_cmx = SQRT(SUM(buf1_cmx)/REAL(n_cmx)) 
              rmsdiff_cmx = SQRT(SUM(buf2_cmx)/REAL(n_cmx)) 
              IF (rmsdiff_cmx > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cmx is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cmx is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cmx /= kgenref_var%cmx), " of ", size( var%cmx ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cmx)/real(size(var%cmx)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cmx)/real(size(kgenref_var%cmx)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cmx 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cmx 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cmx /= kgenref_var%cmx), " of ", size( var%cmx ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cmx)/real(size(var%cmx)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cmx)/real(size(kgenref_var%cmx)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cmx 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cmx 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cmy)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cmy == kgenref_var%cmy)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cmy is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cmy(SIZE(var%cmy,dim=1),SIZE(var%cmy,dim=2))) 
              ALLOCATE (buf2_cmy(SIZE(var%cmy,dim=1),SIZE(var%cmy,dim=2))) 
              n_cmy = COUNT(var%cmy /= kgenref_var%cmy) 
              WHERE ( ABS(kgenref_var%cmy) > kgen_minvalue ) 
                  buf1_cmy = ((var%cmy-kgenref_var%cmy)/kgenref_var%cmy)**2 
                  buf2_cmy = (var%cmy-kgenref_var%cmy)**2 
              ELSEWHERE 
                  buf1_cmy = (var%cmy-kgenref_var%cmy)**2 
                  buf2_cmy = buf1_cmy 
              END WHERE   
              nrmsdiff_cmy = SQRT(SUM(buf1_cmy)/REAL(n_cmy)) 
              rmsdiff_cmy = SQRT(SUM(buf2_cmy)/REAL(n_cmy)) 
              IF (rmsdiff_cmy > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cmy is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cmy is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cmy /= kgenref_var%cmy), " of ", size( var%cmy ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cmy)/real(size(var%cmy)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cmy)/real(size(kgenref_var%cmy)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cmy 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cmy 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cmy /= kgenref_var%cmy), " of ", size( var%cmy ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cmy)/real(size(var%cmy)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cmy)/real(size(kgenref_var%cmy)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cmy 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cmy 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cmz)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cmz == kgenref_var%cmz)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cmz is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cmz(SIZE(var%cmz,dim=1),SIZE(var%cmz,dim=2))) 
              ALLOCATE (buf2_cmz(SIZE(var%cmz,dim=1),SIZE(var%cmz,dim=2))) 
              n_cmz = COUNT(var%cmz /= kgenref_var%cmz) 
              WHERE ( ABS(kgenref_var%cmz) > kgen_minvalue ) 
                  buf1_cmz = ((var%cmz-kgenref_var%cmz)/kgenref_var%cmz)**2 
                  buf2_cmz = (var%cmz-kgenref_var%cmz)**2 
              ELSEWHERE 
                  buf1_cmz = (var%cmz-kgenref_var%cmz)**2 
                  buf2_cmz = buf1_cmz 
              END WHERE   
              nrmsdiff_cmz = SQRT(SUM(buf1_cmz)/REAL(n_cmz)) 
              rmsdiff_cmz = SQRT(SUM(buf2_cmz)/REAL(n_cmz)) 
              IF (rmsdiff_cmz > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cmz is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cmz is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cmz /= kgenref_var%cmz), " of ", size( var%cmz ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cmz)/real(size(var%cmz)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cmz)/real(size(kgenref_var%cmz)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cmz 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cmz 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cmz /= kgenref_var%cmz), " of ", size( var%cmz ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cmz)/real(size(var%cmz)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cmz)/real(size(kgenref_var%cmz)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cmz 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cmz 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cm)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cm == kgenref_var%cm)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cm is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cm(SIZE(var%cm,dim=1),SIZE(var%cm,dim=2))) 
              ALLOCATE (buf2_cm(SIZE(var%cm,dim=1),SIZE(var%cm,dim=2))) 
              n_cm = COUNT(var%cm /= kgenref_var%cm) 
              WHERE ( ABS(kgenref_var%cm) > kgen_minvalue ) 
                  buf1_cm = ((var%cm-kgenref_var%cm)/kgenref_var%cm)**2 
                  buf2_cm = (var%cm-kgenref_var%cm)**2 
              ELSEWHERE 
                  buf1_cm = (var%cm-kgenref_var%cm)**2 
                  buf2_cm = buf1_cm 
              END WHERE   
              nrmsdiff_cm = SQRT(SUM(buf1_cm)/REAL(n_cm)) 
              rmsdiff_cm = SQRT(SUM(buf2_cm)/REAL(n_cm)) 
              IF (rmsdiff_cm > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cm is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cm is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cm /= kgenref_var%cm), " of ", size( var%cm ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cm)/real(size(var%cm)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cm)/real(size(kgenref_var%cm)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cm 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cm 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cm /= kgenref_var%cm), " of ", size( var%cm ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cm)/real(size(var%cm)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cm)/real(size(kgenref_var%cm)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cm 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cm 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cl)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cl == kgenref_var%cl)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cl is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cl(SIZE(var%cl,dim=1),SIZE(var%cl,dim=2))) 
              ALLOCATE (buf2_cl(SIZE(var%cl,dim=1),SIZE(var%cl,dim=2))) 
              n_cl = COUNT(var%cl /= kgenref_var%cl) 
              WHERE ( ABS(kgenref_var%cl) > kgen_minvalue ) 
                  buf1_cl = ((var%cl-kgenref_var%cl)/kgenref_var%cl)**2 
                  buf2_cl = (var%cl-kgenref_var%cl)**2 
              ELSEWHERE 
                  buf1_cl = (var%cl-kgenref_var%cl)**2 
                  buf2_cl = buf1_cl 
              END WHERE   
              nrmsdiff_cl = SQRT(SUM(buf1_cl)/REAL(n_cl)) 
              rmsdiff_cl = SQRT(SUM(buf2_cl)/REAL(n_cl)) 
              IF (rmsdiff_cl > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cl is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cl is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cl /= kgenref_var%cl), " of ", size( var%cl ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cl)/real(size(var%cl)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cl)/real(size(kgenref_var%cl)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cl 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cl 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cl /= kgenref_var%cl), " of ", size( var%cl ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cl)/real(size(var%cl)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cl)/real(size(kgenref_var%cl)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cl 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cl 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cd)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cd == kgenref_var%cd)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cd is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cd(SIZE(var%cd,dim=1),SIZE(var%cd,dim=2))) 
              ALLOCATE (buf2_cd(SIZE(var%cd,dim=1),SIZE(var%cd,dim=2))) 
              n_cd = COUNT(var%cd /= kgenref_var%cd) 
              WHERE ( ABS(kgenref_var%cd) > kgen_minvalue ) 
                  buf1_cd = ((var%cd-kgenref_var%cd)/kgenref_var%cd)**2 
                  buf2_cd = (var%cd-kgenref_var%cd)**2 
              ELSEWHERE 
                  buf1_cd = (var%cd-kgenref_var%cd)**2 
                  buf2_cd = buf1_cd 
              END WHERE   
              nrmsdiff_cd = SQRT(SUM(buf1_cd)/REAL(n_cd)) 
              rmsdiff_cd = SQRT(SUM(buf2_cd)/REAL(n_cd)) 
              IF (rmsdiff_cd > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cd is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cd is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cd /= kgenref_var%cd), " of ", size( var%cd ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cd)/real(size(var%cd)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cd)/real(size(kgenref_var%cd)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cd 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cd 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cd /= kgenref_var%cd), " of ", size( var%cd ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cd)/real(size(var%cd)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cd)/real(size(kgenref_var%cd)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cd 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cd 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%chi)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%chi == kgenref_var%chi)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%chi is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_chi(SIZE(var%chi,dim=1),SIZE(var%chi,dim=2))) 
              ALLOCATE (buf2_chi(SIZE(var%chi,dim=1),SIZE(var%chi,dim=2))) 
              n_chi = COUNT(var%chi /= kgenref_var%chi) 
              WHERE ( ABS(kgenref_var%chi) > kgen_minvalue ) 
                  buf1_chi = ((var%chi-kgenref_var%chi)/kgenref_var%chi)**2 
                  buf2_chi = (var%chi-kgenref_var%chi)**2 
              ELSEWHERE 
                  buf1_chi = (var%chi-kgenref_var%chi)**2 
                  buf2_chi = buf1_chi 
              END WHERE   
              nrmsdiff_chi = SQRT(SUM(buf1_chi)/REAL(n_chi)) 
              rmsdiff_chi = SQRT(SUM(buf2_chi)/REAL(n_chi)) 
              IF (rmsdiff_chi > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%chi is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%chi is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%chi /= kgenref_var%chi), " of ", size( var%chi ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%chi)/real(size(var%chi)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%chi)/real(size(kgenref_var%chi)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_chi 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_chi 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%chi /= kgenref_var%chi), " of ", size( var%chi ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%chi)/real(size(var%chi)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%chi)/real(size(kgenref_var%chi)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_chi 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_chi 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (ALLOCATED(var%cpmin)) THEN 
          dtype_check_status%numTotal = dtype_check_status%numTotal + 1 
          IF (ALL(var%cpmin == kgenref_var%cpmin)) THEN 
              dtype_check_status%numIdentical = dtype_check_status%numIdentical + 1 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank == 0) THEN 
                      WRITE (*, *) trim(adjustl(varname)), "%cpmin is IDENTICAL." 
                  END IF   
              END IF   
              check_result = CHECK_IDENTICAL 
          ELSE 
              ALLOCATE (buf1_cpmin(SIZE(var%cpmin,dim=1),SIZE(var%cpmin,dim=2))) 
              ALLOCATE (buf2_cpmin(SIZE(var%cpmin,dim=1),SIZE(var%cpmin,dim=2))) 
              n_cpmin = COUNT(var%cpmin /= kgenref_var%cpmin) 
              WHERE ( ABS(kgenref_var%cpmin) > kgen_minvalue ) 
                  buf1_cpmin = ((var%cpmin-kgenref_var%cpmin)/kgenref_var%cpmin)**2 
                  buf2_cpmin = (var%cpmin-kgenref_var%cpmin)**2 
              ELSEWHERE 
                  buf1_cpmin = (var%cpmin-kgenref_var%cpmin)**2 
                  buf2_cpmin = buf1_cpmin 
              END WHERE   
              nrmsdiff_cpmin = SQRT(SUM(buf1_cpmin)/REAL(n_cpmin)) 
              rmsdiff_cpmin = SQRT(SUM(buf2_cpmin)/REAL(n_cpmin)) 
              IF (rmsdiff_cpmin > kgen_tolerance) THEN 
                  dtype_check_status%numOutTol = dtype_check_status%numOutTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cpmin is NOT IDENTICAL(out of tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_OUT_TOL 
              ELSE 
                  dtype_check_status%numInTol = dtype_check_status%numInTol + 1 
                  IF (kgen_verboseLevel > 1) THEN 
                      IF (check_status%rank == 0) THEN 
                          WRITE (*, *) trim(adjustl(varname)), "%cpmin is NOT IDENTICAL(within tolerance)." 
                      END IF   
                  END IF   
                  check_result = CHECK_IN_TOL 
              END IF   
          END IF   
          IF (check_result == CHECK_IDENTICAL) THEN 
              CONTINUE 
          ELSE IF (check_result == CHECK_OUT_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cpmin /= kgenref_var%cpmin), " of ", size( var%cpmin ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cpmin)/real(size(var%cpmin)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cpmin)/real(size(kgenref_var%cpmin)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cpmin 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cpmin 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          ELSE IF (check_result == CHECK_IN_TOL) THEN 
              IF (kgen_verboseLevel > 2) THEN 
                  IF (check_status%rank ==0) THEN 
                      WRITE (*, *) count( var%cpmin /= kgenref_var%cpmin), " of ", size( var%cpmin ), " elements are different." 
                      WRITE (*, *) "Average - kernel ", sum(var%cpmin)/real(size(var%cpmin)) 
                      WRITE (*, *) "Average - reference ", sum(kgenref_var%cpmin)/real(size(kgenref_var%cpmin)) 
                      WRITE (*, *) "RMS of difference is ", rmsdiff_cpmin 
                      WRITE (*, *) "Normalized RMS of difference is ", nrmsdiff_cpmin 
                      WRITE (*, *) "" 
                  END IF   
              END IF   
          END IF   
            
      END IF   
      IF (dtype_check_status%numTotal == dtype_check_status%numIdentical) THEN 
          check_status%numIdentical = check_status%numIdentical + 1 
      ELSE IF (dtype_check_status%numOutTol > 0) THEN 
          check_status%numOutTol = check_status%numOutTol + 1 
      ELSE IF (dtype_check_status%numInTol > 0) THEN 
          check_status%numInTol = check_status%numInTol + 1 
      END IF   
  END SUBROUTINE kv_bemt_types_bemt_outputtype 
    
END MODULE BEMT_Types
!ENDOFREGISTRYGENERATEDFILE
